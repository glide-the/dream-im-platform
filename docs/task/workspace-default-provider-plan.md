<!-- [Input] Actual Dream default Workspace helper and coordinator stage_admin-default-workspace-domain plan. -->
<!-- [Output] Primary-owned provider handoff with registered76 and independent public validation boundary. -->
<!-- [Pos] Independent provider task slice; shared ingress/advertisement remains Admin task-owned. -->
<!-- [Sync] 2026-09-15: record candidate21 and registered ingress20/static pass; public checks remain pending. -->

# Default Workspace provider slice

## 背景与问题

Dream default Workspace actor resolvers still call PostgreSQL. The actual helper returns the owned oldest Workspace ordered by created_at/id, otherwise commits a random UUID with the original default label and empty settings.

## 目标与边界

Root owns new workspaceDefault DTO/Repository/Service/tests/source adapter and centralized policy. Admin producer owns shared Route/Receipt/Registry registration after gates; Dream consumes only actual advertised contracts. Root is not alone in the codebase and protects all other dirty modules. No schema/migration, registered codec, launch75 or normal service modification belongs to this slice.

## 概念与规则

Prospective workspace-default.ensure accepts only {} with OAuth dream:write and returns {workspace_id:string}; legacy IDs remain text. Canonical bigint remains decimal into typed parameters. Actor initialization serializes in the Admin UOW. Original result/receipt/audit are atomic; same request replay and GET validate current owned Workspace and null entity scopes before bounded recovery. No actor/settings/name/table/SQL or entity grant is accepted. Identity and existing unified schema requirements are exact, separate from API capability. Workspace status is not a new filter.

Primary Prompt Architect planning is stored in Dream coordinator docs/stage/stage_admin-default-workspace-domain.md before implementation. Existing AGENTS and actual Cursor rules were read; missing Agent.md/docs/rules/README.md/CLAUDE.md are recorded. Stale Cursor schema adapter guidance cannot override packages/db schema and Admin Drizzle authority; no DDL is needed.

Candidate files are initially unregistered. Unit/source/type/lint, registered public concurrency/permissions/UOW/receipt and Dream consumer/no-PG runtime gates remain pending separately. Real Google/account/entity/model acceptance is not performed by this provider-free slice.


## Actual candidate gate

Dedicated Luna raw receipt `/private/tmp/ink-workflow-read-validation/workspace-default-first-gate.md`: unit20/source1 all21, full tsc, owned ESLint, Python AST and diffcheck all exit0. Root read actual stdout. Source invokes the original helper with fixed positional DB results/UUID and captures fresh/legacy/error bigint-safe parameters/commit/rollback; it executes no SQL or PostgreSQL. No cache cleanup, normal service/account/Google/model/runtime or public contract occurred. All files remain unregistered while the75 window is frozen.

Producer ingress calls `executeWorkspaceDefaultEnsure(tx,serviceId,requestId,input,{principal,threadScope:null,runScope:null,editorSessionScope:null})` inside exact identity/unified UOW after live OAuth write checks. Shared original GET must verify stored input digest of{}, null entity scopes and current owned result through `requireOwnedWorkspaceDefaultResult`, using only original operation/request. Complete old75 descriptors and independent PFreceipt/delegation bytes must remain unchanged when registering this component.

## Registered76 handoff

Producer actual generator76 retains full old75 descriptor/requirements/capabilities and independent Preflight/delegation bytes; exact new API SHA `5fb0f70b1790979687090e6c04dd837f24ff0a4c0598d95d5085117e65aa2b95`, exact identity1/unified0033, OAuth dream:write only. Root reviewed actual new ingress11/originalGET9 tests20/no skips plus whole type/lint/diff exit0 at `/private/tmp/ink-workflow-read-validation/workspace76-registration-first-gate.md`. Root candidate21 was not repeated or changed functionally. Local source registration is separate from deployment/public contract/Dream consumer/no-PG closure. Current recipe/harness and isolated public acceptance remain pending.
