<!-- [Sync] 2026-09-16: record exact legacy Google adoption, real callback return and closed Better Auth audience-array validation. -->
<!-- [Sync] 2026-09-16: record completed normal database/ACL activation and keep external auth/model acceptance as separate gates. -->
<!-- [Sync] 2026-09-16: index Registry191 final source closure and separate it from normal activation/real acceptance. -->
> The generated operation artifact contains 191 descriptors. Registry170-174 own Deck Plugin control, Registry175-182 Claude Plugin persistence, Registry183-184 builtin plugin reconciliation, and Registry185-191 Story Workspace artifact authority/index transactions. Dream source closure and the named local normal-database migration/role/ACL activation are complete; exact legacy Google adoption and the real callback return passed, while full Device, logout/expiry and Dream model/business acceptance remain partial.

<!-- [Sync] 2026-09-16: mark the numbered pre-unified Dream PG documents as history and point current readers to the Admin-only boundary. -->
> `ink-dream-memory/01`–`05` now state that Dream psycopg/Alembic/Repository/role material is historical. Current production persistence is Dream Pydantic DTO → Admin Zod DTO → Domain Service → typed Repository → Drizzle, with no Dream database credential or fallback.

<!-- [Sync] 2026-09-16: distinguish the final Admin-only database boundary from dated Registry consumer-pending history. -->
> Current cross-project state: Dream production persistence consumes the named Admin DTO/ORM operations and has no PostgreSQL credential or runtime database path. Dated `pending` statements below are retained as implementation history; normal Google/Device/model/business acceptance remains a separate deployment gate.

<!-- [Sync] 2026-09-16: index Registry170-174 Deck Plugin control-plane DTO/ORM ownership. -->
The generated operation artifact now contains 174 descriptors. Registry170-174 preserve the Registry169 prefix and append five Deck Plugin control operations. Admin derives the OAuth actor, checks Workspace ownership or canonical instance-admin role, computes immutable plans and commits revision-checked installation/materialization changes with the original receipt and audit. Dream retains local artifact verification, shared filesystem and CLI behavior. Existing unified and local-placement capabilities are sufficient; no migration is added. Registry169 prefix SHA is `adb90cec21e76f709d9d10638642051f33b1eb04df618f6984aaffb5c4a0962e`; full Registry174 SHA is `242ddb8e06c66058b4a6a0a015a08edb41353446ae00be46e6c2f1b946cf57dc`.

<!-- [Sync] 2026-09-16: index Registry169 automatic-repair message settlement. -->
The generated operation artifact now contains 169 descriptors. Registry169 preserves the Registry168 prefix and adds one actor-scoped `dream-auto-repair.settle` operation. A closed DTO, domain Service and typed Drizzle Repository compare the full immutable repair identity before changing only the terminal status in the receipt/audit transaction. Initial insertion reuses `chat-user-message.persist`; Dream keeps validation, retry orchestration, Runtime, SSE and shared filesystem work. Existing unified schema capability is sufficient, so no migration is added.

<!-- [Sync] 2026-09-16: index Registry148-168 Notion connector DTO/ORM ownership and generated contract. -->
The operation artifact now contains 168 descriptors. Registry148-168 keep the Registry147 prefix byte-identical and move five-table Notion connector persistence into strict Zod DTOs, one domain Service and typed Drizzle Repository. User calls derive their actor from OAuth or an exact Runtime grant; scheduled calls use `connectors:sync` and connector-derived owner identity. Dream keeps Notion remote execution, credential/shared-file handling, sync policy and Agent Runtime. Existing unified schema capability is reused and no migration is added.

<!-- [Sync] 2026-09-16: index Registry134-147 managed-MCP DTO/ORM ownership and generated contract. -->
The operation artifact now contains 147 descriptors. Registry134-147 keep the Registry133 prefix byte-identical and move managed MCP configuration, App settings, encrypted credential storage, discovery cache and import receipt persistence into strict Zod DTOs, one domain Service and typed Drizzle Repository. OAuth and exact Runtime delegation remain separate actor forms; Dream keeps MCP protocol discovery, external provider OAuth, credential encryption/decryption, Agent Runtime and product interaction. The focused 23-test, TypeScript and lint gates pass; Dream consumer and isolated PostgreSQL/normal business acceptance remain pending. No migration was added.

<!-- [Sync] 2026-09-16: document Registry130-132 Dream launch current/frozen Runtime ownership. -->
The operation artifact now contains 132 descriptors. Registry130-132 keep the Registry129 prefix byte-identical and move launch scope, current/frozen binding derivation and Runtime metadata preparation into Admin DTO/Service/typed Drizzle. Dream verifies shared artifact bytes and Claude CLI locally, while Admin owns Run/binding/lock joins, materialization, current-mode Workspace installation and the write receipt. The restricted-role PostgreSQL contract passes eight scenarios after all 63 migrations. No schema migration was added.

<!-- [Sync] 2026-09-16: document Registry127-129 Agent-type Runtime metadata ownership and Dream local artifact verification. -->
The operation artifact now contains 129 descriptors. Registry127-129 preserve the complete Registry126 prefix and move Chat clear plus Runtime plan/prepare persistence into Admin DTO/Service/typed Drizzle. The Runtime candidate omits paths; Dream verifies shared bytes, manifest and CLI, then Admin rechecks evidence and owns materialization, Workspace installation and binding CAS. The restricted-role PostgreSQL contract passes seven scenarios after all 63 migrations. No schema migration was added.

<!-- [Sync] 2026-09-16: index Registry122-126 Deck Plugin binding DTO/ORM ownership and isolated PostgreSQL evidence. -->
<!-- [Sync] 2026-09-16: index Registry121 claim-bound confirmation authority, migration and Runtime fencing. -->
<!-- [Sync] 2026-09-16: index Registry120 confirmation DTO/ORM operations, service claim scope and isolated PostgreSQL evidence. -->
<!-- [Sync] 2026-09-15: index Registry115 Story guidance DTO/ORM operation and isolated PostgreSQL evidence. -->
<!-- [Sync] 2026-09-15: index Registry114 Story catalog DTO/ORM operations and isolated PostgreSQL evidence. -->
<!-- [Sync] 2026-09-15: index Registry111 Story review DTO/ORM operations and isolated PostgreSQL evidence. -->
<!-- [Sync] 2026-09-15: record both Dream Story output consumers of unchanged Registry109. -->
<!-- [Sync] 2026-09-15: index Registry109 standalone Story output DTO/ORM/UOW and original receipt boundary. -->
# Architecture inventory

The normal-database auth/data ACL release path is now implemented as a separate dry-run-first command. It requires a private 0600 manifest, verified stopped-cluster backup digest, exact database identity, all 63 migrations, eight published capabilities and explicit `--production-approval`; it creates three NOINHERIT service logins plus one Dream NOLOGIN role, reuses the isolated ACL planner and runs actual credential/allow/deny probes. No normal database activation is claimed by this code change. Sync 2026-09-16.

The operation artifact now contains 126 descriptors. Registry122-126 preserve the complete Registry121 prefix and append five OAuth Deck Plugin binding operations. Strict Zod DTOs, one Service and typed Drizzle repositories own current/history/options/validation plus atomic binding CAS; Dream keeps the public product routes and the separate agent-type Runtime/shared-filesystem path. The restricted-role PostgreSQL contract passed all five scenarios after all 63 migrations. No schema migration was added. Sync 2026-09-16.

The Registry121 prefix remains byte-identical. It preserves Registry120 and appends only service-scoped `story-workspace-confirmation.claim-turn`. Admin claims the durable message and creates or recovers one exact message/claim-bound `server-persistence` grant in the same UOW. Migration 0062 adds nullable binding columns, FK/check/partial-unique constraints and `identity.runtime-confirmation-claim.v1`; existing OAuth delegation rows remain compatible. Every resolve/renew checks the live claim and database-clock lease, so ACK, replacement or expiry fences later ORM work. Sync 2026-09-16.

The operation artifact now contains 120 descriptors. Registry120 preserves the complete Registry115 prefix and appends OAuth confirmation submit/fact plus service-only claim/lease/ack. Closed DTOs, one Service and a typed Drizzle Repository move confirmation ownership, Run transitions, Chat message persistence and durable delivery state to Admin; Dream retains shared-filesystem validation and same-Thread Runtime. The named restricted-role PostgreSQL contract passed lifecycle, replay, claim, concurrency, owner and secret-table ACL cases. Existing schema is sufficient; no migration was added. Sync 2026-09-16.

The operation artifact now contains 115 descriptors. Registry115 preserves the complete Registry114 prefix and appends only OAuth `story-workspace-guidance.submit`. The closed DTO, Service and typed Drizzle Repository move Run/Workspace/Thread authorization, immutable guidance-message persistence, idempotency and receipt handling into Admin while Dream retains same-Thread Runtime dispatch. The runner-owned restricted-role PostgreSQL contract passed all five cases after applying all 62 migrations. No new migration was added; normal business acceptance and other Dream database domains remain pending. Sync 2026-09-15.

The operation artifact at Registry114 contains 114 descriptors. Registry114 preserves the full Registry111 prefix and appends only OAuth `story-workspace-catalog.workspace`, `story-workspace-catalog.read` and `story-workspace-catalog.patch`. Closed DTOs, one Service and a typed Drizzle Repository move the remaining 11 public Story Workspace catalog routes to Admin while preserving the Dream payloads, filters, ordering, pagination, relations and patch rules. The runner-owned restricted-role PostgreSQL contract passed all six cases after applying all 62 migrations. No new migration was added. Sync 2026-09-15.

The operation artifact now contains 111 descriptors. Registry111 preserves the full Registry109 prefix and appends only OAuth `story-workspace-review.transition` and `story-workspace-review.batch`. Strict DTOs, one Service, a typed Drizzle Repository and the existing receipt/audit UOW move all eight Dream review routes to Admin while retaining Dream's public response shapes. The isolated restricted-role PostgreSQL contract passed six scenarios for ownership, cascade, batch order, idempotency, concurrent conflict, rollback and user-table ACL. No migration was added; normal business acceptance and remaining Story SQL are pending. Sync 2026-09-15.

Registry109 remains the 109-descriptor operation artifact. Dream now consumes `story-workspace-output.store` from both the public Agent post-turn owner and the authenticated internal Story output REST route; this consumer closure changes no DTO, hash, Drizzle schema or migration. Sync 2026-09-15.

The operation artifact now contains 107 descriptors. Registry107 preserves the complete Registry106 prefix and appends only `workflow-managed-mcp-scope.resolve`, an actor/Run/Thread-bound `dream:read` projection implemented as strict DTO → Service → typed Drizzle Repository. Admin returns the owned `workspace_id`; Dream retains managed MCP snapshot loading, Runtime, SSE and shared filesystem operations. Sync 2026-09-15.

The operation artifact now contains 106 descriptors. Registry106 preserves the complete Registry105 prefix and appends only `deck-workspace-plugins.resolve`, a Thread-bound `dream:read` metadata aggregate. Admin derives the actor, checks Thread/Deck ownership, reads enabled refs and configured Story adapter status with strict DTO and Drizzle ORM; Dream retains shared-filesystem artifact verification and workspace packing. Sync 2026-09-15.

The operation artifact now contains105 descriptors. Registry105 preserves the complete Registry104 prefix and appends only `deck-chat-context.resolve`, an OAuth `dream:read` with strict Deck/Voice selectors, null entity grants and exact identity/unified requirements. Admin owns actor permission filtering and ordered Deck/Voice/plugin-ref storage projection, including enabled and installation status, in one read-only DTO → Service → typed Drizzle Repository path. Dream owns enabled/ready decisions, prompt composition, artifact verification, workspace, Runtime and SSE. Sync 2026-09-15.

The operation artifact now contains104 descriptors. Registry104 preserves the complete Registry103 prefix and appends only `deck.default-plugin.resolve`, an OAuth `dream:read` with exact identity/Deck requirements and no receipt. The map binds Dream default-plugin lookup to this Admin read while preserving the existing `deck.create` and `deck.reconcile-default` transaction ownership; Dream consumer completed in Dream commit `06df48cd`; normal acceptance remains pending. Sync 2026-09-15.

Registry103 preserves the complete Registry101 prefix and appends only current-actor `picture-history.list`/`picture-history.full`, both OAuth `dream:read` with exact identity/unified requirements and no receipt. The map binds all three legacy Dream picture routes to these two Admin operations while Dream consumer and normal acceptance remain pending. Sync 2026-09-15.

The operation artifact now contains101 descriptors. The two-operation local-data suffix preserves the complete Registry99 descriptor prefix and reuses the existing identity/unified schema capabilities. Its map binds the legacy aggregate and first-login transactions to typed Admin operations while Dream adapter and normal acceptance remain pending. Sync 2026-09-15.

The operation artifact now contains99 descriptors. The new sixteen-operation Reflections suffix preserves the canonical hash of all prior83 descriptors and leaves the separate delegation and Preflight receipt artifacts byte-identical. The reviewed source artifact is now marked `registered_registry99_source`; it carries exact OAuth7/background9 scopes, int4 event bounds, worker high-water recovery and the new persistence capability hash. Generated Registry99 SHA is `2f5af5acb5c52864a3ff00ce19e05540e5191b6f33450f5d2bc4911b5ac7f96e`, map SHA `dbb9604d2168af1a7cf8a14c7b04c7895c79899828d1423b780d151f2248c08a`, delegation SHA `8a9b9b123593e8297eb167144a1e90fabe9d9a32fdf5ff115cc9171dae14dc49` and Preflight receipt SHA `2a8d994fc8ae0c1047e9465c2a94ec0a0cbf3f4deba16f90468da4727aa86ad3`. Public PostgreSQL/fault/ACL and Dream consumer evidence remain pending. Sync 2026-09-15.

The existing Registry83 `session.list` descriptor is unchanged. Its internal ingress now admits only a fully resolved same-service `server-persistence` grant with `dream:read`, a live canonical owner/Thread binding and no Editor binding; the domain repeats the exact actor-shape restriction and all other Session operations remain closed to that purpose. Independent Luna focused29, whole cache-free TypeScript, focused lint, Markdown and diff gates passed; isolated public and Dream consumer evidence remain pending. Sync 2026-09-15.

The operation artifact now contains83 descriptors. The three Reflections section-config additions preserve all prior80 full descriptors plus the separate Preflight receipt/delegation artifacts. Get/save/delete and bounded original write recovery passed corrected57/57 static and named isolated public/fault/final-COMMIT recovery gates. Dream consumer and normal account/model acceptance remain pending. Sync 2026-09-15.

Current unified identity/data ownership is [Admin/Dream contract](admin-dream-auth-data-contract.md); exact implemented wire DTOs are generated from [operation registry artifact](admin-dream-operation-contracts.json). [Domain map](admin-dream-domain-implementation-map.md) preserves full candidate and transaction evidence with pending closure; partial API/auth foundation is never full production migration proof. Historical schema-authority documents retain prior decisions with current ownership annotations.

[Delegation artifact](admin-dream-delegation-contracts.json) freezes the actual create/renew/revoke purpose and Editor Session DTOs separately from named data operations. Catalog/role/public-Route/consumer readiness remains required before activation.

[Preflight original receipt artifact](admin-dream-workflow-preflight-receipt-contract.json) describes its dedicated three-state recovery protocol separately from the named operation registry, currently83. Later operation appends preserve its bytes. Stage checking evidence does not resume an orphan or refresh a committed result. Source binding/snapshot fresh/reuse and full projection/PFT checks passed; full isolated public/stage evidence remains pending.

[Auth architecture](auth.md) and [device interaction](auth-device.md) describe actual login/register/Google/Session/product paths and the remaining external/real-user acceptance gates. Unknown Better Auth plugin/ORM failures rethrow to the Admin transaction boundary for rollback and a generic no-store503 without logging exception objects. They link the common topology and validation matrix rather than introducing a second acceptance checklist.

`admin-dream-data-ownership.md` records the concrete identity/public-table/dream-request ownership decision, exact existing DATA table footprint, limited projections and actual restricted-role evidence; physical whole-domain schema migration and normal credential activation are not claimed.

PF raw stored signing35 and stage-audit key20 gates passed without wire/DDL changes. First complete24 failures and remediation expiry failure remain recorded; four independent isolated fault cases passed96 with exact original projections and stage2/2/4/3 retention. Unregistered Run create/retry source/atomic/service25 passed, while shared output character correction, wire registration and public/consumer acceptance remain pending.
Current Registry72 adds original atomic Run create/retry after primary released the PF70 window. All old70 full contract/requirement/capability objects and independent original receipt/delegation bytes match. Registration gate47+key/display2+bounded-helper1=50 passed with type/lint/diff0; public harness static gate passed Route2/source-clock1 with whole type/lint/diff0. Owner-only target-directory proof and minimal source-child environment await their focused supplement; public creation remains pending. Launch source independently passed19 deterministic+4 actual source=23 and ingress5 tests after preserving harness interpreter/schema-export failures; it remains unregistered and awaits public validation and complete prepare/dispatch metadata. Historical partial counts above are stage receipts, not current registry totals.

Later Run72 supplement/source1 and erased environment-type correction passed. Root reviewed original full public exit1 and remaining22 denial/10GET/230 assertions exit0, six prior complete bounded results and eight core business tables preserved. Three naturally expired unexecuted negative operands use new current PF/request facts with exact old frozen tuples and retained old expiry. This is independent scope evidence, not one full28 pass; atomic faults/unknown-COMMIT and real business remain pending. Unregistered dispatch claim/finish preserves separate original transactions and pure metadata numeric/time encoding; its new domain22/source4/ingress6 gate passed32 with whole type/lint/Python syntax/diff0. These partial launch APIs remain outside Registry72.

Current artifact/map75 adds source/claim/finish with all prior72 full descriptors and independent PFreceipt/delegation bytes unchanged. Actual application NULL-Agent fingerprint omission was corrected before registration; actual application/identity2 plus affected GET24 and type/lint/AST passed. Public thin POST3/GET7 passed10; new SELECT-only public component harness static type/lint passed, execution pending primary-owned isolated facts. Later Run72 fault/actual-COMMIT business68 and cleanup3 supersede the earlier pending sentence: original business command exit1 for its cleanup-helper diagnostic remains preserved. Full prepare/failure/Runtime/Google/normal Launch22 and all remaining domains remain pending; current registration is not deployment or real-business evidence.
<!-- [Sync] 2026-09-15: record Registry99 Reflections state and no-log auth boundary; all-domain/normal acceptance incomplete. -->

`admin-dream-reflection-task-operation-candidate.json` retains the reviewed sixteen-operation source snapshot now registered as the Registry99 suffix. Aggregate canonical SHA is `2af7477c424a92c39a1d323b301ef698da147dfa4f1aeb4e8a2166f146981365`; raw file SHA is `74513bb643d6a7210d437beb14a46766a9e7ce6285c85fa7fe1004ad558c279f`. Its drift test recomputes every Zod schema, int4 event boundary, operation hash and requirement. Report save uses the existing ingress body capacity while list/snapshot/authority capacities remain explicit server configuration. Sync 2026-09-15.
<!-- [Sync] 2026-09-16: generated operation inventory now includes Registry183-184 builtin reconciliation. -->
`admin-dream-operation-contracts.json` contains 184 descriptors. Registry183-184 retain the Registry182 SHA and expose only `plugins:catalog` service operations for builtin installation/ref reconciliation. The full Registry184 SHA is `f71ac328ad7670298d518e388b2fde89033387f97ac35b91a0ea66da487b40cd`.
