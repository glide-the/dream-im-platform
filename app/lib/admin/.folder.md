# app/lib/admin — Folder Contract

- 本目录承载 Admin Session/RBAC 后的服务端资源校验、事务、SQL 与审计；`app/api/admin/**` Route Handler 保持薄层。
- `claude-plugin-marketplaces.ts` 是平台全局 Remote Marketplace 控制面：严格 Zod、精确 capability、HTTPS host allowlist、临时 Git checkout、immutable revision/entry、批准/阻断与审计。
- `claude-plugin-marketplaces.test.ts` 锁定 URL/ref 校验、manifest 解析与完整内容 digest；`claude-plugin-marketplaces.integration.test.ts` 以显式开关读取公开 Comfy Git，复用同一生产 inspector 且不写业务数据库。
- Remote Marketplace v1 只接受仓库内相对目录插件 source；符号链接、路径逃逸、凭据 URL、非 allowlist host、并发同步和无完整插件 digest 的批准请求全部 fail closed。
- 同步目录在任务结束后删除；业务表只保存 URL/ref/commit/manifests/digests/validation/policy，不保存临时路径、bucket key 或用户 scope。
- `claude-agent-resources.ts` 是 Claude Agent 资源控制台的唯一服务端边界：固定 setting、strict Zod、DB-clock heartbeat freshness、`system.read/write`、Origin、乐观 revision、行锁与同事务审计；desired 与 Dream snapshot 的最大并发都只接受正整数至 PostgreSQL int4 硬边界，不接受无限 sentinel；GET 读取与 PATCH 写事务都必须精确验证数据库 capability，PATCH 在任何 desired 写入前 fail closed；禁止 Admin 到 Dream HTTP/Bearer、部署或进程控制。
- `guard.ts` 对所有 mutation 始终要求 Origin；缺失、非法或不在 request origin/显式 allowlist 中一律 403，不按部署环境名称降级。

## Sync

- 2026-08-27: PostgreSQL projection and optimistic desired updates share the positive-int4 concurrency contract; exact capability validation remains inside the write transaction before any setting or audit write.
- 2026-08-19: Added global ClaudePlugin Remote Marketplace CRUD/sync/revision/policy handlers and live Comfy inspector coverage. Admin and Dream share capability hash `d215cb2764f656ab32e364a4900b3aac73fca60c77ef4c9f3a914fd192a8c314`.
