# PostgreSQL schema contract

- `schema.ts` 与 `schema/**` 是指向 `packages/db/src/schema/**` 的兼容 facade；唯一 Drizzle schema 来源、连接池、内嵌 PostgreSQL 与 migration runner 均由 `@ink-memory/db` 拥有。
- 默认业务阈值必须来自版本化 policy 配置，并通过新增前向 migration 落库；已记录 migration、snapshot、tag、when 与 hash 永不修改。
- 全量业务数据回填不得塞进 schema migration，必须使用 `drizzle/data/**` 的显式、可审计 runner，并先在具名隔离 PostgreSQL 验证。
- schema 变更必须同步更新文件头/目录合同、migration contract tests、文档与数据库 capability 证据。
- `claude-plugin-marketplace-schema-contract.test.ts` 校验五张全局表、不可变/并发约束、零用户 scope、无对象存储关系和精确 capability metadata。
- `dream-managed-mcp-schema-contract.test.ts` 校验四张 MCP 关系、NULL-safe scope、密文凭据边界、可保留的成功导入指纹和精确 capability hash。
- `claude-agent-resource-observer-schema-contract.test.ts` 校验 latest-instance snapshot 列、时间/JSON 约束、heartbeat index、0040 journal 与精确 capability hash。
- `claude-code-runtime-schema-contract.test.ts` 校验 `ai_models` 两个 nullable positive int4 字段、0041 additive migration/journal 与 exact Runtime capability；Dream 不拥有 DDL。
- `chat-history-keyset-pagination-schema-contract.test.ts` 校验 0042 的 `chat_message(thread_id, created_at DESC NULLS LAST, id DESC NULLS LAST)` 索引、非覆盖大 JSON 边界、journal 与 exact Dream capability。

## Sync

- 2026-09-02: Drizzle 0042 publishes `dream.chat-history-keyset-pagination.v1` only after the exact stable-order index exists; `parts`/`metadata` remain outside the index.
- 2026-08-28: Drizzle 0041 publishes `dream.claude-code-runtime-config.v1` after additive model Runtime columns and positive checks.
- 2026-08-27: `dream.ts` 与 Drizzle `0040` 新增 PostgreSQL-only Claude Agent observer snapshot 合同。
- 2026-08-25: `dream.ts` 与 Drizzle `0038` 新增 Admin-owned Dream MCP 资源合同；不包含 UI/API、凭据加解密或 runtime DDL。
- 2026-08-21: 数据库所有权迁入 Paperclip-style `packages/db`，应用目录只保留稳定导入路径。
- 2026-08-19: `dream.ts` and Drizzle `0037` add five platform-global ClaudePlugin Remote Marketplace tables, nullable operation/installation lineage, immutable revision/entry triggers, full-plugin digest checks, one-running-sync uniqueness, RBAC permission, and `dream.claude-plugin.remote-marketplace.v1` capability. No user/tenant scope or object-storage relation is introduced.
- 2026-08-16: `dream.ts` adds Deck aggregate draft revisions and append-only
  content snapshots; Dream consumes them only through the
  `dream.deck-content-versions.v1` capability published by the matching
  forward Drizzle migration.
