<!-- [Sync] 2026-09-16: Registry120 moves Story Workspace confirmation facts, Run transitions, Chat message persistence and durable delivery claims into Admin. -->
Registry120 appends OAuth `story-workspace-confirmation.submit/fact` and service-only `claim/lease/ack` after the complete Registry115 prefix. Closed Zod DTOs expose only the confirmation command, owned Run lookup and exact delivery-claim identities. Admin derives the actor and source Thread, performs permission checks and Run transitions, and commits message/result/receipt through a typed Drizzle Repository; fixed advisory-lock and database-clock expressions are the only raw SQL. New submissions alone return an immediate dispatch envelope, while business replays rely on the durable claim reconciler. Dream keeps shared-filesystem validation, same-Thread Runtime delivery, EventBus and SSE and has no confirmation PostgreSQL fallback. The five operation SHAs are `2571aa2cc9c19656c4ac90d33221da65e8a631657adebf9f535ab0fe3c76bb12`, `f455a6075161751d25a229dd64479e2a6d6ca781ea7aacfa5575ec4561f52beb`, `c049317c4383584a7574b11daea1b8c626875d589e0dbbd45dfb739c4ca8cde1`, `a5720992e5a0cbc39773481dd3f98a32e6b535c34ea24df230de6ad5646817e6` and `12aeed9beb6584354aa584ebadf7ce352d68084632c0d2c90a2c768c3a8626c6`; Registry115 prefix SHA stays `58ab3cd933165dca7d6ae2d6eb50f46ff8f148e8e7eaf3dd5e46ceab1ad2ba9b`, full Registry120 SHA is `4b0bbfa8caecd42acf0ecc89be4153b6d6fb1e124aac4ff27e937ecb14904795`. Existing schema is sufficient; no migration is added.

<!-- [Sync] 2026-09-15: Registry115 moves Story Workspace guidance authorization, idempotency and message persistence into Admin. -->
Registry115 appends OAuth-only `story-workspace-guidance.submit` after the complete Registry114 prefix. Its closed Zod DTO accepts only a Workflow Run ID, guidance kind, optional text or step ID and idempotency key. Admin derives the actor, Workspace, source Thread, immutable message ID, canonical fingerprint, parts and metadata; the typed Drizzle Repository locks the owned Run/Workspace/Thread, accepts only confirmed or failed Runs, serializes the message identity and commits the new message, Thread touch, result, audit and receipt in one UOW. Exact request replay returns the original result, business replay does not reorder the Thread, changed content conflicts, and only a newly committed command carries a dispatch envelope for Dream Runtime. The operation SHA is `a061ed38d2ca10073bbb7fd078e679f072f0cbd4ff1ce900792fbf8725223727`; Registry114 prefix SHA remains `dc80b77410aac58528dde77578154d9848d9dfc3bf55de4a35c8c315a81af704`; full Registry115 SHA is `58ab3cd933165dca7d6ae2d6eb50f46ff8f148e8e7eaf3dd5e46ceab1ad2ba9b`. Existing schema is sufficient. Sync 2026-09-15.

<!-- [Sync] 2026-09-15: Registry114 moves Story Workspace catalog browse/edit persistence into strict DTO/Drizzle operations. -->
Registry114 appends `story-workspace-catalog.workspace`, `story-workspace-catalog.read` and `story-workspace-catalog.patch` after the frozen Registry111 prefix. Closed Zod unions expose only default Workspace ensure/patch, six Story/Character/Scene list/detail views and controlled resource patches. OAuth supplies the actor; the typed Drizzle Repository owns filters, stable ordering, pagination, relations, row locks, owner checks and Scene-to-owned-Story validation. Reads have no receipt. Writes commit the safe DTO projection, entity audit, operation audit and original receipt in one UOW; retry with the same input replays, changed input conflicts, and Dream never falls back to PostgreSQL. Registry111 prefix SHA remains `01f1a9ffbd9daf44e9bc640768a13ae636d9e718cb42365ff2de1ba5c244efc3`; full Registry114 SHA is `dc80b77410aac58528dde77578154d9848d9dfc3bf55de4a35c8c315a81af704`. No migration is required. Sync 2026-09-15.

<!-- [Sync] 2026-09-15: Registry111 moves Story Workspace review transitions and batch review into strict DTO/Drizzle transactions. -->
Registry111 appends `story-workspace-review.transition` and `story-workspace-review.batch` after the frozen Registry109 prefix. OAuth supplies the actor; strict DTOs allow only the closed Story/Character/Scene actions and IDs. The typed Drizzle Repository enforces owner, generated, pending and archive predicates, keeps Story artifact revision checks, performs Story confirmation cascades, and commits each updated row, per-item audit and operation receipt in one UOW. Batch preserves request order, returns explicit updated/skipped IDs and never accepts actor, SQL, table, column, path or transaction selectors. Dream retains the eight public route shapes and has no PostgreSQL fallback. No migration is required. Sync 2026-09-15.

# Dream data domain service

Registry109 appends only `story-workspace-output.store` after the frozen Registry108 prefix. Its strict DTO contains a source Thread and one Dream-parsed Story proposal; actor, Workspace, database, SQL, table, column, path and transaction selectors are rejected. Admin derives the actor from OAuth or the exact Thread delegation, serializes actor initialization with the existing default-Workspace policy, locks the owned Thread, and uses a typed Drizzle Repository to reconcile Story identity, named Characters, ordered Scenes and all join rows. The Story graph, counts, pending review state, operation receipt and audit commit in one UOW; original-receipt recovery revalidates its Thread/result scope and rejects Run or Editor bindings. Dream retains proposal parsing, Agent Runtime, EventBus, SSE and shared filesystem behavior. No migration is required. Sync 2026-09-15.

Registry108 appends only `workflow-runtime.activate` after the frozen Registry107 prefix. The strict DTO contains the source Thread, Workflow Run, observed SDK Thread reference and Dream-verified plugin identity fields; it rejects actor, workspace, database, SQL, path and placement selectors. Admin derives the actor from OAuth or the exact Thread/Run `server-persistence` delegation, locks the owned Run, validates the frozen Runtime lock and Admin installation through typed Drizzle repositories, then commits materialization refresh, immutable load receipt and entries, the existing creating-to-active Agent Session lifecycle, queued-to-running CAS, transition, operation receipt and audit in one UOW. Active Run replay revalidates materialization, Session and observed SDK Thread bindings without requiring a current installation or writing new lifecycle rows. Placement, creating lease and built-in adapter policy come from `DREAM_RUNTIME_ACTIVATION_POLICY_JSON`. Dream retains launch-manifest byte verification, Agent Runtime, EventBus, SSE and shared filesystem behavior. No migration is required. Sync 2026-09-15.

Registry107 appends only `workflow-managed-mcp-scope.resolve` after the frozen Registry106 prefix. The strict Run/Thread DTO enters one capability-checked read UOW; Admin derives the actor from OAuth or an exact Thread/Run delegation and the typed Drizzle Repository joins the Run to its owned Story Workspace. The response contains only the same Run, source Thread and `workspace_id`. Dream retains managed MCP snapshot loading, MCP use, Runtime, SSE and shared filesystem behavior. Sync 2026-09-15.

Registry106 appends only `deck-workspace-plugins.resolve` after Registry105. Its strict Thread/profile DTO, Service and typed Drizzle Repository reuse the owner-filtered runtime ref read and return only plugin metadata needed by Dream's workspace packer. The Story Workspace adapter is selected from `DREAM_WORKSPACE_PLUGIN_POLICY_JSON`; Admin returns latest/ready status facts in the same read UOW, while Dream keeps artifact verification, shared-filesystem copies, freeze/repair, manifest, Runtime and SSE. Sync 2026-09-15.

Registry105 appends only `deck-chat-context.resolve` after the frozen Registry104 prefix. Its strict selector DTO, `DeckChatContextService`, typed Drizzle `DeckChatContextRepository` and read-only data UOW return the actor-owned Deck plus selected/all Voice and plugin-ref storage rows in deterministic order, including enabled and installation status fields. OAuth `dream:read`, null entity grants and exact identity/unified capabilities are required. Admin derives the owner and performs permission filtering; Dream applies Deck/Voice/ref enabled and plugin-ready rules, prompt composition, byte truncation, workspace, Runtime and SSE behavior. The read has no receipt, write, filesystem access or migration. Sync 2026-09-15.

Registry104 appends only `deck.default-plugin.resolve` after the frozen Registry103 prefix. Its strict empty DTO and OAuth `dream:read` service derive package/version from `DREAM_DECK_POLICY_JSON`; the typed Repository returns the newest ready exact match by `created_at DESC, id DESC`, projected to six fields, or null. The read has no receipt, audit, migration or filesystem/CLI action. Existing `deck.create` and `deck.reconcile-default` continue to recheck installation ID, configured package/version, digest and ready status inside their write transactions. Dream consumer replacement completed in Dream commit `06df48cd`; normal acceptance remains pending. Sync 2026-09-15.

Registry103 appends `picture-history.list` and `picture-history.full` after the frozen Registry101 prefix. Strict ISO-date DTOs, `PictureHistoryService`, typed `PictureHistoryRepository` and the existing data UOW bind every row to the OAuth actor. List uses inclusive optional bounds, descending date order and thumbnail fallback while preserving nullable prompt/timestamp; full returns the newest same-date original image or null. The reads perform no friendship check, receipt, migration or filesystem action. Dream consumer replacement and normal acceptance remain pending. Sync 2026-09-15.

Registry101 appends OAuth `local-data.import` and `first-login.complete` after the frozen Registry99 prefix. Strict raw-JSON DTOs, one Service, typed `LocalDataImportRepository` and the existing receipt/audit UOW preserve the four-category transaction without caller owner or physical selectors. All Session IDs are locked and checked before writes; same-owner rows update and any foreign owner rejects the whole aggregate. Pictures and Reports append once per committed receipt, imported report time is normalized RFC3339 and stored in `created_at`, and the visible preference count remains zero through four non-empty fields. First-login insert/update/repeat returns the same stored completion. A runner-owned loopback PostgreSQL proved rollback, concurrency and unknown-COMMIT recovery; no migration is required. Dream consumer and normal acceptance remain pending. Sync 2026-09-15.

Registry99 appends sixteen Reflections task/result/event/report operations after the canonical-hash-stable Registry83 prefix. Seven operations use live service-bound OAuth; nine use the exact `reflections:execute` service scope. OAuth and task-scoped background writes have separate original receipt paths. The `rta_` authority enters only six existing handlers after exact service/subject/task/section/Thread/liveness checks; Session list additionally requires `include_text=false`, and child write recovery repeats the same subject and Thread binding. Worker restore returns the persisted event high-water mark, and event append replays an identical record or accepts only the locked task's current max+1 within PostgreSQL int4. Registry artifact SHA is `2f5af5acb5c52864a3ff00ce19e05540e5191b6f33450f5d2bc4911b5ac7f96e`; map SHA is `dbb9604d2168af1a7cf8a14c7b04c7895c79899828d1423b780d151f2248c08a`. Public PostgreSQL/fault/ACL, Dream consumer and normal business validation remain pending. Sync 2026-09-15.

The existing `session.list` operation now accepts one additional actor shape: an exact opaque bearer that `DelegationService.resolve` validates for the current configured service, `dream:read`, live ownership and `server-persistence` purpose, with a Thread binding and no Editor Session binding. The domain service repeats that shape check and denies every other Session operation. OAuth Session access and the existing exact `editor-stdio` load/replace path remain explicit actor shapes. No operation, DTO, schema, hash or Registry83 artifact changed. Independent Luna focused29, whole cache-free TypeScript, focused lint, Markdown and diff gates passed; isolated public/consumer acceptance remains pending. Sync 2026-09-15.

Registry83 appends only `reflections-section-config.get/save/delete` after the frozen Registry80 prefix. The existing strict DTO/Service/typed Repository and handler retain service-bound live OAuth, null entity scopes, raw stored JSON bytes, Dream-owned filtering/default/effective/FS/Agent behavior, and same-UOW receipt/audit for writes. Original GET exposes only bounded save/delete results for the same service and subject. Corrected focused validation passed57/57 with whole type/owned lint/AST/JSON/Markdown/diff gates; generated artifact SHA is `2ce9712bf6d5d16867ae4cc2d68c167b834cacee45d25b647d1ea18f7367d566`. All prior80 full descriptors and independent Preflight/delegation bytes remain unchanged. Initial50/57 is retained: importing the receipt-bearing service from Registry caused a cycle, and the source command used a missing interpreter; Registry now supplies the same requirement constants without that import, and the corrected command uses the verified project venv. Named isolated public validation completed48 logical requests plus258 preservation and2568 transaction/recovery assertions: five injected write/receipt/audit faults fully rolled back all125 relations, and save/delete each recovered one actual final-COMMIT response loss without duplicate effects; product `console.error` remained0. Dream consumer and normal model validation remain pending. Sync 2026-09-15.

Registry80 previously appended only `user-system-config.get`, `user-system-config.patch` and `thread-system-config.get` after released77. User management is service-bound OAuth with all entity scopes null; patch locks the existing preferences row and commits config/result/receipt/audit in one UOW. Thread read repeats current owner and permits only OAuth or a `server-persistence` grant for the same Thread with null Editor and a matching authoritative nonnull Run. The fixed Python codec preserves raw JSON numeric categories and unknown stored keys without caller path/executable selection. Focused static tests passed45/45; its generated artifact SHA was `661823a292301a67b70ccff8068492a035cc6a3b90fe7abe4eb623f4bc161879`. Prior77 full descriptors, separate Preflight/delegation bytes, and both frozen Failure77 harnesses were unchanged. Public PostgreSQL/concurrency/fault/preservation and Dream consumer/normal model validation remain pending. Sync 2026-09-15.

Historical Failure77 POST/default real fixed-codec/GET registration11 and whole type/ownedlint/AST/diff passed; existing Next config4 passed, packaging build unexecuted. Public harness static type/lint/diff passed, with221 Markdown/MDX,356 decoded local links,247 historical routes and0 missing. Producer sent explicit PUBLIC READY and refroze77 production/public harness. PriorSource7/domain35/Handler7 were unchanged. Reflections3 then remained unregistered with domain17/Handler8/source1batch11=26 tests/type/lint/AST/diff PASS; defaults/effective/FS remain Dream. Sync 2026-09-15.

Historical Registry77 appended independent `dream-launch-failure.envelope` after all released76 FULL descriptors and unchanged PFreceipt/delegation bytes. Thin default handler bound the fixed server failure codec, shared original GET checked OAuth write/current owner/immutable input/full original scope, and Next traced both original launch and failure scripts. Registration and public-harness static gates passed; source/domain semantics retained earlier7/35/Handler7 evidence. At that stage Reflections3 and SystemConfig were unregistered; later current state is Registry83 above. Sync 2026-09-15.

Historical Workspace76 independent fault90 and SELECT-only preservation159 passed after public347: three INSERT boundaries fully rolled back protected17, final-COMMIT single response loss recovered original GET/replay with one effect/receipt/audit, six owned fault objects were cleaned and old125/first-preparation17 fullrows/five positive originals were retained. At that stage failure/Reflections remained candidates; later current state is Registry83 above. Normal business/model/Runtime/FS stayed unexecuted. Sync 2026-09-15.

`reflectionsSectionConfigDto/Repository/Service/Handler` implement the three registered83 custom-prompt operations. OAuth-only owner/section SQL, exact identity/unified, all-null entity scopes and same-UOW write receipt/audit are required. Save accepts the original Dream postfiltered five-file nonempty string record as raw serialized JSON; get retains legacy object/unknown/numeric bytes and original missing/invalid/nonobject null or blank `{}` fallback, while delete retains bool/no-op. Static default/effective merging/display/FS/Agent stay in Dream. Source/domain/ingress/registration static and named isolated public/fault gates passed; Dream consumer remains pending. Sync 2026-09-15.

`reflectionsSectionConfigOriginalReceiptService` is the registered83 save/delete recovery boundary. Ingress supplies a live OAuth-write principal and exact schema UOW; the service repeats active principal/name/request/scope checks and reads only original service/subject/name/request receipts. Complete bounded result, null Thread/Editor/Run scopes and stored digest format are required. It never looks up or reapplies later prompts/defaults/effective state; original prompt input equality remains POST's existing receipt boundary. Recovery15/15, corrected57/57 static, public recovery, five rollback faults and two actual final-COMMIT response-loss cases passed. Dream consumer remains pending. Sync 2026-09-15.

`dreamLaunchPrepareSource.test.ts` and the fixed `dreamLaunchPrepareOracle.py` compare23 complete source vectors for actual whole prepare/Agent-scope/replay. First source exit1 was oracle command alias mismatch; explicit original wire aliases and successful replay query-key correction passed1 batch test/23 vectors, lint/AST/diff. Original PermissionError capture supplement also passed1 batch/23 vectors and lint/AST/diff, retaining its first malformed lint-wrapper exit1 before corrected ESLint exit0; earlier failure/PASS remain separate. Catalog, current-scope and frozen-runtime/binding are captured collaborators; actual SQL/Gateway/FS/provisioning remain unexecuted. No named prepare operation, production76 or primary-owned SystemConfig is changed. Sync 2026-09-15.

`dreamLaunchFailureHandler` is an independent unregistered ingress candidate during the76 freeze. It accepts only the strict failed-Run/workspace/error envelope, requires live OAuth or original Run-bound persistence actor, and adds exact immutable delegation/purpose capabilities for grants. The mandatory server-only pure overlay is forwarded to the existing independent UOW service; no fixed-map/Trace/Route/Registry advertisement or Runtime activation. New Handler7 and whole type/owned lint/diff passed, earlier35 domain/recovery tests unchanged; public/wiring remains pending.

Current76 registration adds primary-owned `workspace-default.ensure` through producer `workspaceDefaultHandler` and `workspaceDefaultOriginalReceiptService`. Both use exact identity/unified and service-bound OAuth write authority; POST strict empty input initializes through the existing same-UOW helper, GET checks empty input digest/all null scopes/current canonical ownership before returning the full original legacy text ID. New ingress11/original-domain9 and whole static gates passed; prior75 FULL descriptors and PFreceipt/delegation bytes match saved snapshots. Primary actual isolated public14 cases/22GET/347 assertions and protected17 full-state checks passed, including first same/distinct-original concurrency and whole original helper. Independent fault90/actual COMMIT-loss and preservation159 passed; primary released76 freeze. No new DDL or default policy change; failure candidate remains unregistered. Root's default candidate21 source/unit and normal business acceptance remain separate.

Admin owns this PostgreSQL pool, strict DTO projections, capability checks and atomic repositories. Requests authenticate a configured service, then the operation-specific user or background scope. No SQL, table name, user ID, credentials or entity serialization crosses the domain API boundary.

`resourceDto.ts`, `resourceRepository.ts`, `resourceService.ts` and `resourceHandler.ts` provide off-path policy reads and diagnostics publishing. `operationRegistry.ts` defines input/output versions and contract hashes. See [shared contract](../../../docs/architecture/admin-dream-auth-data-contract.md).

`chatThreadDto/Repository/Service/Handler` preserve exact decimal bigint IDs, PG microsecond/NULL keyset semantics, immutable message envelopes/final projections, owner/CAS checks and original message+thread transaction. Fourteen operations have provider-free isolated public-Route evidence. Required unified capability follows immutable 0033's current hash, not 0032's superseded hash.

`receipts` commits business/result/audit together and binds original service/actor/operation/request/input plus entity scope; duplicates recover the original DTO, conflicting input is 409, no absent receipt implies timeout rollback. `principal` preserves entity scope for runtime bearers; scoped credentials cannot create/list/delete unrelated threads. `userProfile*` projects the current OAuth owner's profile/provider labels without hashes/tokens and preserves timestamps/IDs.

`editorSessionDto/Repository/Service/Handler` implement closed EditorEngine state and named Session save/get/batch/list/text-list/delete. Owned upsert rejects foreign Session IDs; Editor replace preserves last-write semantics. Missing load is explicit null; invalid stored full state is unavailable, while legacy metadata previews omit malformed prose. Public Editor load/replace/receipt accept only the exact Session-bound Editor bearer, without service/DB credentials. Internal `session.list` additionally accepts only a fully resolved same-service `server-persistence` grant with `dream:read`, live owner/Thread checks and no Editor binding; the other Session operations remain OAuth-only. Receipts independently bind Editor and Thread scopes. Forward 0057 expands purpose/Session fields; 0058 validates nullable purpose/array ambiguity and publishes exact capability. Isolated replay/catalog and restricted-role 8-operation public Route/fault contract passed (103 assertions); the new list authority has focused static evidence while consumer/real-business acceptance remains separate.

`workflowContextDto/Repository/Service/Handler` derive active context from an owner-scoped Thread and the complete indexed linear retry graph, frozen binding/workspace/source provenance and Python-compatible launch hashes. No request run/actor/Deck selector acquires authority. Valid terminal leaves yield ordinary Chat only after all integrity checks. New runtime grants compare the requested Run to that authoritative context; existing grants retain original-owner validation for final persistence. Context31/delegation20/capability5 and type/lint passed; prepared restricted public5 families passed170 assertions. Registry108 now owns assembled Runtime activation; remaining Workflow output/confirmation commands are not complete.

`confirmationGuard/Repository` lock existing control facts after the owned Thread lock, verify derived message/command/Run/workspace identity, and preserve newer valid durable claims. `userMessageDto/Service/Handler` expose atomic raw user persistence/title with same-UOW receipts; Dream retains its pure attachment/text title candidate algorithm. Generic user-message persistence also calls the guard based on DB classification. Latest focused87 tests, typecheck and lint passed; prepared restricted public5 families passed170 assertions.

`deckVoiceDto/Repository/Service/Handler` expose 19 named aggregate operations with strict OAuth ownership, draft/version checks, raw numeric-preserving JSON and atomic receipts. Actual public validation found JSONB negative-zero normalization; forward0059 canonical-text storage and its exact capability preserve new hash bytes while retaining legacy NULL compatibility. Isolated migration/catalog and all19 restricted public operations passed246 assertions, including reciprocal ordered Voice locks. Consumer/refs/runtime/real acceptance remains separate. Entity grants cannot manage Decks.

`deckContentCanonical` is a fixed Admin pure-stdlib codec preserving existing numeric/Unicode snapshot/hash semantics via raw business JSON text. SQL/UOW remain typed repositories; helper stdin/stderr/body are not logged, timeout fails closed. Docker carries Python and the fixed script; Next traces that source.

`workflowRunDto/Repository/Service/Handler` expose Run read and ordered transition history with the original complete lifecycle validation, joint Session/receipt bindings, source tuple rules and microsecond ordering. Queries repeat original actor/workspace scope and current workspace ownership; existing Run grants must match the original Run and source Thread. Original Run/Transition service timestamps normalize to UTC with six exact microseconds independently of PG session offsets; shared Chat/Deck projection retains its existing behavior. Corrupt lifecycle/history is unavailable, while a missing owned Run is 404. Focused22 tests, whole typecheck and owned lint passed; complete restricted public5 Run validation passed163 assertions. `workflowRunCommand*` implements narrow start/fail/cancel with owned Run/workspace locks, persisted readiness/placement/raw lock hash/creating Session validation, original status CAS and one appended transition. Start requires OAuth; persistence grants only reduce state or recover bounded results. New commands use same-UOW receipt/audit with independent exact Run/Thread scopes; Focused55 tests, whole typecheck and owned lint passed; latest UTC correction focused40 and complete public5 Run operations163 assertions passed. Preflight/create/retry/output/confirmation commands are not yet advertised.

`deckRuntimeDataDto/Repository/Service/Handler` add six coordinator-owned refs/Voice metadata operations. Ref list/prepare/replace and actor analysis require OAuth; runtime refs and Voice memory use exact Thread ownership/grants. Bound installation evidence matches shared locked metadata/compatibility before replacement and draft change. Voice default repair changes only the Voice, with the original Thread receipt scope. Pure raw memory inspection preserves original Python decoding. Focused37 tests/type/lint passed; public6 passed104 assertions after explicit OAuth-only entity-bearer403 correction. Real artifact/CLI consumption remains pending.

`workflowPreflight*` and `workflowTokenAuthority` register owner-only read/execute and centralized pft HMAC/UTC microsecond authority using explicit Admin `INK_WORKFLOW_TOKEN_SECRET`. No JWT fallback or entity bearer grants new Run creation tokens. Focused43 tests, whole typecheck and lint passed; read retains identity/unified requirements. Execute additionally requires immutable request capability0060. Full read/execute public/stage evidence and Run create/retry remain pending.

`userPreferencesDto/Repository/Service/Handler` register the two coordinator-owned current-owner preference operations with raw config-object JSON and five-field COALESCE merge, preserving NULL/blank strings/first-login/time semantics. OAuth-only writes use one UOW for merge/result/audit and original receipt recovery; first-login and system configuration are outside the write DTO. Focused19 tests and new60 ingress whole typecheck/owned lint passed; public2 passed78 assertions; normal business acceptance remains pending.

`deckRuntimePluginLockDto` reuses complete Manifest encodings for original nested lock constraints/defaults and validates legacy datetime inputs without re-encoding raw hash bytes. `deckPluginCompatibilityDto/Repository/Service` prepare the original eight checks, safe first-failure actions, five-domain capability intersection and Python Unicode ordering. Runtime-context and ready-installation selection remain distinct; context uses ANY matching historical materialization, while Preflight smoke checks use the latest row. Explicit server compatibility flags default to incompatible. `workflowPreflightDependencies/DependencyRepository/DependencyService` prepare narrow original binding/profile, immutable snapshot/hash and required-entry/smoke checks with Deck-before-Voice locks; caller owns transaction boundaries. Model normalization preserves Pydantic White_Space separately from explicit Python strip; the fixed codec preserves three raw approved-capability decoders. Source parity tests skip in the normal provider-free suite unless both `INK_DREAM_SOURCE` and `INK_DREAM_ORACLE_PYTHON` are explicit; the dedicated parity stage uses that complete read-only interpreter, while two metadata model probes retain `python3` only as a local fallback. Phase10 nine files116 tests (including actual source oracle), whole typecheck/lint passed; these new dependencies are not registered or publicly verified yet.

All other domain groups remain tracked in [implementation map](../../../docs/architecture/admin-dream-domain-implementation-map.md); this partial API registry is not full Dream production DB closure.

`workflowPreflightExecutionDto/Repository/Service` implement immutable original-request reservation and original checking/binding/snapshot/final stage commits using the Phase10 authoritative dependencies. `workflowPreflightSecretReceipt` encrypts and verifies the original complete result and all service/subject/input/PF/canonical/workspace bindings. Concurrent original checking lookup returns in-progress evidence without resuming an orphan; committed replay recovers bounded expiry and consults original receipt before a failure mutation after uncertain final commit. No Run/Session/Runtime is created. First deterministic55 and added38 receipt/ingress/secret tests passed. `workflowPreflightOriginalReceiptService` adds durable three-state original lookup with a request lock before association/receipt reads, current workspace/canonical checks and encrypted committed recovery. Actual source input/fingerprint11 cases, complete projection/PFT4 cases and public recovery ingress8 checks passed, whole type/lint0. Execute is registered70 with dedicated original receipt protocol; full isolated public/stage validation remains pending.

`socialFriendshipDto/Repository/Service/Handler` register nine coordinator-owned original invitation/decision/relationship and relationship-authorized picture reads. OAuth-only principal and identity/unified capabilities precede domain UOW; writes preserve original result/receipt/audit atomically, reads do not require generation policy. Explicit policy carries original6-character/7-day rules plus server collision budget. Coordinator focused25/type/lint and actual source22 cases passed; all9 restricted public operations passed230 assertions. Prior60 descriptors and exact coordinator9 hashes stay unchanged in the70 operation artifact.

Actual original CompatibilityService19-case oracle found pure ASCII control source_ref passes Pydantic model stripping but fails the original explicit Python blank check. Service lock resolution now applies stripPythonString before blank predicates; manifest/lock DTO/hash remain unchanged. Initial source assertion failure is retained; minimal19-case rerun and existing18 compatibility tests passed. Forward0060 isolated catalog/replay and latest ACL15 actual-role attempts passed; production pool actual-role checks and normal activation still pending.

Actual raw stored lock/snapshot/input-hash whitespace source comparison found that original Preflight signs raw fields before output normalization. Read, reuse and final signing now use `issueStored`, normalizing only expiry to original UTC microseconds; source/read/execution/token35 tests and whole type/lint passed. First public24 failed at binding: actual read-only catalog proved stage audit rows collided under the existing request/action/resource-type/resource-id unique index. Stage audit keys now include only the fixed server stage in the scoped digest, retaining request correlation, action and same-stage uniqueness. New repository5 plus execution15 tests, whole typecheck and owned lint passed. Original failed PF/receipt/history remain intact; new-request public continuation and stage-fault validation remain pending. No wire descriptor or immutable DDL changed.

`workflowRunCreationDto/Repository/Service/Semantics` prepare unregistered atomic create/retry with original raw frozen-source/hash semantics, owner/source/Preflight checks, token CAS, scoped semantic replay, two initial transitions and result/audit in one UOW. Retry explicitly rolls back its read-only prerequisite then repeats terminal/source facts under lock in the clean write UOW. The original Unicode key policy is shared by the new input; existing shared output still awaits its codepoint correction after the frozen PF contract window. Creation/source/atomic three files25 tests, whole typecheck and owned lint passed; actual original fresh/fresh-semantic/consumed source parameters/full projection/commit parity included. No create/retry entry is advertised or publicly verified yet.

`workflowRunCreationHandler` prepares unregistered OAuth-only ingress on exact identity/unified capabilities. Every initial/retry-read/creation UOW rechecks the same live subject/canonical/client with write authority. No route or registry dispatch exposes it during PF freeze; Ingress9 tests, whole typecheck and owned lint passed independently of the already passed25 source/atomic/service tests. Shared output codepoint/wire preview and public registration remain pending.

Primary actual isolated compatibility/snapshot INSERT/final passed UPDATE/snapshot binding UPDATE faults passed96 assertions with original full response parity and retained stage audits2/2/4/3. Already committed snapshots survive the latter two failures; Run/Session delta0. Owned injected triggers/functions were cleaned and original failure history retained. Full complete24/remaining-read/other stage faults and real acceptance remain separate.

Remaining-read harness explicitly marks only two mandatory unconsumed expiry facts; extra consumed/expired-state/permission reads retain complete original projections and no-write checks. Initial classification failure is retained; marker correction type/lint passed, actual remaining public continuation is pending separately from producer code.

`dreamLaunchSourceDto/Semantics/Repository/Service/Handler` prepare unregistered hidden launch source ensure. Server actor/workspace/key derive the original UUIDv5 Thread/message and canonical content fingerprint; original command boundaries, Unicode title and metadata are explicit protocol policy. Current enabled Deck/workspace scope and stored source validation precede bounded receipt recovery. Source Thread/message/time/result/audit share one UOW; replay retains dispatch envelope and ordering. Run/PF creation and Runtime dispatch are separate original transactions. New actual-source/deterministic validation is pending; no Registry/Route dispatch exposes this API during PF freeze.

Later primary Preflight continuation passed remaining8 reads74, actual-final-COMMIT loss/original recovery44, exact own-child checking/binding/snapshot SIGKILL recovery54 and original permission tail11. Original failed complete24/classification receipts remain; independent scopes do not claim a single full24 pass. No normal DB/service, old expiry or expected response was modified; no orphan resume or Run/Session creation occurs.

Primary has released PF freeze. Run stored key now uses original255 codepoints with identical JSONschema metadata; create/retry are registered72 and dispatch to the OAuth-only CreationHandler. All old70 full descriptors, independent Preflight receipt and delegation artifact bytes are equal. Creation original GET repeats current workspace/Run owner, immutable/frozen source and actual source message/time before bounded recovery. New lifecycle/receipt/ingress47 plus key/display source2 and bounded helper1 tests passed (50), type/lint/diff0; public creation/retry remains pending. Earlier unregistered/pending notes above are historical stage evidence.

Launch source first gate retained incorrect interpreter ENOENT and missing schema-export diagnostic; 19 deterministic tests passed. Root reused the existing workspace export, and only four source cases reran using the actual project interpreter: all4 passed, whole type/lint/diff0. Aggregate23 tests establish original command/UUID/hash/fresh/replayed ensure-source full result/INSERT/commit parity. Separate Handler5 ingress tests and owned lint/diff passed; public registration remains absent from Registry72. Agent/model/binding preparation and claim/finish/failure metadata remain separate pending launch boundaries.

Run72 public harness static gate passed thin Route2 and original clock/retry-frozen source1 with whole type/lint/diff0. Narrow owner-only target proof/minimal child supplement passed source1/lint/diff0. Explicit remaining_denied_receipts retains full-default assertions, skips accepted POST/source flows and checks preserved bounded originals before remaining denial/GET checks. First type gate recorded Next ProcessEnv overload diagnostics; erased type casts preserve the exact minimal environment, corrected whole typecheck passed. Primary exclusively owns public fixtures and execution; Registry72 and old contract bytes stay stable during that window.

`dreamLaunchDispatchDto/Codec/Repository/Service/Handler` prepare unregistered claim/finish metadata. Current workspace owner, frozen binding, Run creator and complete deterministic source facts derive the original context and envelope; metadata unknown numeric categories remain raw Python canonical JSON. Workspace/binding/Run precede message locks. Claim/result/audit commit before Dream Runtime; accepted finish maps only pending/dispatched in a separate UOW and stale claims cannot overwrite current leases. Bounded claim recovery requires its active stored claim, parts and context. `fixedDomainCodec` is new pure transport with a fixed codec map and PATH-only environment; registered canonical transport remains unchanged. Provider-free Service22/source4/Handler6 tests all32 passed, including complete actual dispatcher/independent finish canonical parameters/commit and original context/lease vectors; whole type/owned lint/Python syntax/diff0. Preparation, failure recorder and public/full launch remain pending; these APIs stay unregistered.

Primary released the Run72 fault window after independent business proof68 and SELECT-only cleanup3; the business command retained exit1 for a primary lexical cleanup harness failure, not a production defect. `dreamLaunchOriginalReceiptService` prepares unregistered original source/claim/finish reads, reusing current POST source/frozen/lease validators. It derives workspace from owned storage and denies expired claim recovery; no caller provenance selectors or entity grants. New original GET and extracted-helper source gates are pending before Route/Registry/75 artifact registration.


## Primary-owned unregistered default Workspace slice

| File | Pos | Function |
|---|---|---|
| `workspaceDefaultDto.ts` | default Workspace DTO | Closed empty ensure input and legacy text ID result. |
| `workspaceDefaultRepository.ts` | typed Workspace repository | Actor initialization lock, owned oldest query and original default insert; no pool/commit. |
| `workspaceDefaultService.ts` | Workspace UOW service | OAuth-only identity, atomic result/audit and current-owner original replay validation. |
| `workspaceDefault.test.ts` / `workspaceDefaultSource.test.ts` | provider-free candidate checks | Closed fields/owner/entity/failure and actual Python helper parameters/commit parity. |

Sync 2026-09-15: candidate remains unregistered; source/unit/type/lint and actual public concurrency/UOW gates pending.

## Launch75 current registration

Actual Registry/artifact/map now contains75; all prior72 full descriptors and independent Preflight/delegation artifact bytes match saved snapshots. Production Route delegates closed source/claim/finish, and original GET reuses current source/frozen/active-claim validators. Getter24 and extracted actual-source2 initially passed but type gate recorded one mock-type diagnostic; later actual application callsite exposed the NULL-Agent fingerprint omission. Fixed source identity omits that payload key, actual application-source2 plus affected GET24 and whole type/lint/AST passed; original failed comparison remains in the raw receipt. Earlier helper-only hash vectors are historical and cannot establish full application parity.

Registration publicPOST3/GET7 (10) passed with whole typecheck/owned lint/diff exit0. New SELECT-only `adminDreamLaunch.contract.ts` static type/lint/diff passed; primary-owned strict0600 recipe/target fixtures and public component execution remain pending. Protected17-table snapshots and actual original application/ensure/dispatcher/finish comparisons are harness assertions, not yet public SQL evidence. Registered production75 and harness bytes remain frozen for that primary window; historical unregistered file-head markers will be synchronized after release. Full Agent/model/binding prepare, failure recorder, Runtime and real Launch22 remain pending.

During the75 freeze, independent `dreamLaunchFailureEnvelope.py` and `dreamLaunchFailureSource.test.ts` prepare only the original pure failed-metadata overlay and whole recorder/real transition source checks. The actual recorder's lookup/read rollback and FAILED Run COMMIT precede its separate envelope COMMIT; envelope-stage failure preserves the committed Run. Source/codec7 tests and whole type/lint/Python AST/diff passed; no fixed transport-map/Route/Registry/schema mutation or failure API is attached. Later composition must reuse existing Run fail authority and retain current owner, original bounded receipts and separate UOWs.

New unregistered `dreamLaunchFailureDto/Repository/Service` compose only the independent envelope stage. Existing `workflow-run.fail` remains authoritative for the preceding state/history transaction. Workspace SHARE precedes owned Run UPDATE, then the backing source locks; current failed status, canonical owner, exact source time and original Thread/Run grant are required before receipt replay. Mandatory server-only pure codec DI has no fallback or public callable selector; fixed transport wiring awaits release. A missing message is bounded updated=false, while a deleted previously-updated source cannot recover true. Raw parts/time and prior Run failure remain untouched. New domain17 tests and whole typecheck/owned lint/diff passed; no registry/route/schema advertisement.

- Sync 2026-09-15: primary default Workspace candidate unit20/source1 all21, type/lint/AST/diff exit0; production registration/public/consumer and normal acceptance remain pending. No existing module or registered75 bytes changed by this slice.

`dreamLaunchFailureOriginalReceiptService` is a new unregistered recovery candidate. Its bounded output retains the original truthy error so original GET can reconstruct the immutable input digest independently of later mutable metadata. POST and GET share current failed-Run/source ownership, exact tuple/time and result validators; current workspace derives from storage, deleted updated=true cannot recover, original updated=false remains a no-op. It does not overlay metadata or grant Runtime authority. Affected POST17 plus new GET18 (35) and whole typecheck/owned lint/diff passed; no shared Handler/Route/Registry/fixed codec map/schema is touched, and public/consumer acceptance is pending.

`dreamLaunchContinuation.test.ts` holds six pure continuation guards under the existing Vitest app include; it imports only the test-harness helper. First guard gate retained excluded test-path exit1 and inferred optional-status type exit2; correction guard6/6 passed with one later input-type exit2, and fresh typing supplement whole type/lint/diff passed. Primary's first public75 exit1 is retained with only source-new-null committed. The bounded continuation rechecks that original entirely read only, then runs all37 remaining POST cases and all GETs; production75/harness/helper/guards are refrozen and public continuation remains pending.

- 2026-09-15 Root-owned default Workspace candidate is now locally registered76; DTO/service headers corrected only, candidate21 retained. Producer ingress11+originalGET9/type/lint0 are separate; public concurrency/UOW/current-owner acceptance and deployment/consumer remain pending.


<!-- [Sync] 2026-09-15: Failure77 isolated component closed: continuation6/16/27/0/556, atomic126, preservation293 and cleanup passed; primary release acknowledgement pending. -->

Unregistered Root-owned userSystemConfigDto/Repository/Service/Codec.py/userSystemConfig.test.ts: closed10-field user OAuth get/patch, raw Python JSON, first-row lock and isolated-column merge; producer codec map/Handler/OriginalGET and Thread/public gates remain pending.


<!-- [Sync] 2026-09-15: add independent unregistered userSystemConfigSource.test.ts; actual whole get/save14-case parity pending. -->


<!-- [Sync] 2026-09-15: independent SystemConfig unit24/source1batch14 all25/type/lint/AST2/diff exit0; unregistered UI candidate only. -->

Root实际读Luna user-system-config-first-gate.md：new2test files/25passed、wholetsc0、ownedlint0、AST2/diff0；actual whole get/save14params/rawJSON/commit/close和明确nonobject/nonfinite failclosed冲突已验。未接fixedcodec map/Handler/OriginalGET/Registry，Thread/Run Runtime reader、public/concurrency/receipt/consumer与正常真实验收仍pending；不重复旧域门禁。


<!-- [Sync] 2026-09-15: Root unregistered Thread SystemConfig read candidate, no frozen77/shared Repository/wire change. -->

Root-only threadSystemConfigDto/Service/test reuses threadIdInputDto, strict raw output, existing ChatThreadRepository.requireOwned(current-owner read) and UserSystemConfigRepository.get plus mandatory shared read helper/old pure codec. OAuth current Thread or matching persistence Thread/upstream current Run required; Editor/inconsistent scopes denied before data access. Read writes no defaults/Thread/Prefs/receipt/audit; Runtime consumes a future immutable snapshot. New domain/affected25/type/lint/public/consumer gates pending; no registered count/SHA/schema change. Before-code authority: [Dream 仓库](https://github.com/glide-the/im-dream) 的 `docs/stage/stage_admin-thread-system-config-domain.md`.


<!-- [Sync] 2026-09-15: Root Thread configuration read review avoids upgrading upstream grant SHARE locks. -->

Unregistered Thread read uses existing owner SELECT before mandatory raw UserConfig helper; current bound grant keeps its upstream Thread/Run permission locks, with no extra UPDATE-lock upgrade on a pure read. Actual first18/User24 and focused18+source1batch14=19/type/lint0 retained; post-review Thread/type/lint gate still pending. No shared77 Repository/wire/catalog/schema change.

<!-- [Sync] 2026-09-15: Failure77 first public exit1 retained; independent continuation static gate pending. -->

`dreamLaunchFailureContinuation.test.ts` tests only the new harness guards: mandatory partial287/Editor6 evidence, exact six accepted labels, all22 cases/all27 GETs, denial-only POST and full17 historical-row preservation. The production77/first public harness remain frozen. First public failed because the Editor fixture expected ACCESS_SCOPE_REQUIRED while the actual binding-first boundary returned403 DELEGATION_ENTITY_DENIED; SELECT-only diagnosis287 and probe6 independently passed. New independent continuation restores six originals without POST, then executes all16 denied POSTs/all27 GETs. Static and public continuation gates pending; no single22 complete PASS or normal-model claim.


<!-- [Sync] 2026-09-15: Root accepted final ThreadSystemConfig18/cache-free type/lint/diff0, unregistered public/consumer pending. -->

Root已实际读取 thread-system-config-read-lock-review-gate.md：最终Thread18/18 passed、whole cache-free tsc0、owned2file ESLint0、diff0。先前User24 unchanged通过、source1batch14在显式env下通过；首轮env/type/cache失败与修正19均保留。Current-owner SELECT复用既有Repository，避免纯read升级已有grant SHARE锁；所有当前Thread/Run权限仍需future shared Handler/public验证。三新candidate/复用raw helper未注册或接fixedmap/广告schema，Dream Runtime/user config直连消费者未因此关闭。

Failure77 independent continuation first static gate: newguard12/12/no skips and owned3lint/Markdown inventory/diff exit0; whole tsc exit2 due only newtest literal-token inference. Runtime facts unchanged by as-const correction; type supplement pending, public READY not yet sent. Original firstpublic exit1 and protected77 anchor retained.

<!-- [Sync] 2026-09-15: Failure77 continuation static correction accepted; documentation final gate pending. -->

Producer read failure77-continuation-typing-correction-gate.md: cachedpnpm whole tsc --noEmit --incremental false, focused test ESLint and diff each exit0. As-const only correction retained12/12 guard PASS without rerun; first tsc2 remains separate. Independent verifier/helper/test now statically accepted, final document inventory/frozen8 equality still pending before PUBLIC READY; public/atomic/COMMIT-loss/preservation and normal acceptance remain unexecuted by producer.

<!-- [Sync] 2026-09-15: independent continuation PUBLIC READY after actual raw gate review. -->

Actual document/freeze gate read: Markdown221/356/247/0missing, frozen8 changed[] and diff each exit0. Newguard12/12/no skips and focused type correction/ownedlint all passed; first tsc2 retained. Producer sent exact mandatory evidence schema/command and PUBLIC READY to primary, then refroze new verifier/helper and production77/original failed harness. Primary may now prepare independent negatives/current17 anchor and last freshOAuth; actual public16/27/6/0 counts, fault/COMMIT-loss/preservation remain pending. No private fixture/credentials/PG or normal-model execution by producer.

<!-- [Sync] 2026-09-15: independent continuation actual public preflight exit1 retained. -->

Producer read actual node/public-wrapper continuation receipts: both exit1, safe FAIL label preflight/status null/code null. The6 restored-original/16 denied-POST/27GET contract did not complete. Earlier static12/type-correction/lint/docs/frozen8 PASS remains separate; original firstpublic exit1/partial287/Editor6 retained. Actual fresh-negative prepare exit0/23setup retained six positives/full17/oldRun states/oldexpiry with zero positive replay; earlier fixture continuation exit0/55setup/12operands/old125 retained independently. Primary private SELECT-only staged diagnostic requested, no production/harness guard weakening or accepted-index inference. Source remains frozen, public/atomic/preservation and release pending.

Round27 actual diagnosis: producer逐字读取safe sandbox/network command receipts。默认sandbox preflight exit1，stage verification.target_role/classification loopback_permission_denied/assertions0/public_cases0/business_mutation false/normal untouched；同一Root-owned SELECT-only诊断在明确隔离loopback network permission下exit0/23，strict fixture/evidence/coverage/retention与target/三role/fivefalse/dataDir/current17均通过，public_cases0/business_mutation false。归类harness权限前置，不是production/fixture/guard defect；不修改77或new continuation。Root只因300s凭据实际过期准备exclusive新signer/fixture/evidence和2个新的public negative grants，复用已存在queued operand/ordinaryEditor事实，不重演source/claim/positivePOST，不重置oldgrant expiry/Run/graph；随后只在相同scoped-network边界重试冻结continuation。原两次exit1均保留，retry结果未通过前不claim6/16/27。

Round27 scoped-network retry actual acceptance: producer逐字读取actual node command receipt与Luna wrapper receipt。`node --import tsx tests/integration/adminDreamLaunchFailureContinuation.contract.ts` exit0；6个原结果只读恢复、16 denied POST、27原GET、0 positive POST、556 assertions、17 protected tables。First public exit1、first continuation preflight exit1、partial287与Editor6均独立保留；不能表述为单次22全PASS。Whole original recorder already-FAILED source path被恢复验证，prior FAILED Run保持；new FAILED transition、Runtime、provider、FS、normal account/model均未执行。Root retryprep exit0/8仅复用1 queued operand/new graph0/2fresh negative grants/full17 oldrows保留/新300s OAuth last-signed。Producer未读取privatefixture/evidence/credentials或PG正文。Production77、首次harness、新continuation源继续冻结；Primary metadata/receipt/audit fault、actual final-COMMIT-loss/preservation/own cleanup及RELEASE仍pending。

Failure77 final isolated evidence actual raw receipts read: atomic command exit0/126. Faults source_update, receipt_insert and audit_insert each returned503 AUTH_SERVICE_UNAVAILABLE with all17 relations byte-exact rollback and prior FAILED Run preserved; each owned trigger/function pair cleanup passed. Actual final-COMMIT loss injected once: initial503, original GET200 and replay200, exactly one source metadata effect/receipt/audit; whole original recorder and prior Run full row retained. Cleanup removed six owned objects, active fault objects none, history retained. SELECT-only preservation exit0/293 retained original125/prepublic125/postpublic17 full rows, all six positive owner/digest/scopes and complete already-FAILED Run transitions/PF/bindings/history; active fault functions0. Normal database/services, provider/model/Runtime/FS untouched. Together with independent public6/16/27/0/556, registered77 already-FAILED envelope component is isolated-closed; no claim of a newly executed FAILED transition, whole failure application, normal/real model or ALL-domain closure. Production/harness remains frozen until primary explicit RELEASE before Round26 wiring.

## Registered Reflections task aggregate

`reflectionTaskDto/Repository/Service/Handler` define sixteen closed Registry99 operations: seven live OAuth task/report operations and nine exact `reflections:execute` worker operations. The Repository owns task/section/result/event/report SQL and CAS inside the caller UOW. Worker load creates one bounded private Session/config snapshot, derives its workspace locator from server configuration plus task ID and returns `last_event_sequence` so a restarted EventBus resumes the stored stream. Event sequence is the PostgreSQL int4 range; after the service locks the task row, append replays one byte-equivalent event or accepts only current max+1. Section completion replaces results atomically, and report ensure is task-linked exactly once. Shared POST and OAuth/background receipt Routes expose only the registered operations and strict selectors.

`reflectionTaskAuthorityRepository/Service` uses the separate `dream.reflection_task_authorities` table for an encrypted `rta_` bearer bound to service, OAuth subject, canonical owner, task, section and child Thread. Its machine allowlist is exactly user-message persist, assistant-message persist, Thread get/update-session, Thread system-config get and Session list. The real handlers pass their exact operation to the resolver; cross-Thread, other Chat/Session/config operations and Session text listing fail before domain storage. Child write receipt lookup derives the same authority subject and requires the stored Thread scope. Service-authenticated begin replay can renew the same recovered bearer after the short TTL only until its original maximum; revocation, maximum expiry and terminal task/section reject recovery. Dream still must add the explicit Reflections persistence owner/keeper, drain before finish/revoke and keep ordinary Chat `idg_` behavior unchanged. The bearer never enters Claude CLI/MCP arguments, workspace files, logs, receipts or audits.

The reviewed source artifact is retained as the Registry99 source snapshot. Earlier tests and the owned disposable PostgreSQL harness proved source parity, the 0061 old-writer lock, five-row deterministic resequence, seven post-cutover rejection classes and cleanup. Registration tests now cover exact prefix/hash parity, Route dispatch, separate original receipts, all six real authority handler entries, cross-Thread/text/nonallowlist rejection, event restart/high-water/max+1 and no-insert hole behavior. 0061 was not rerun during registration. Service UOW faults, role ACL, Dream consumer and normal business/model acceptance remain pending. Sync 2026-09-15.
