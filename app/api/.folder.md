# API ingress contract

Registry104 routes `deck.default-plugin.resolve` through a dedicated OAuth `dream:read` Handler. Its strict empty input opens the existing identity/Deck capability-checked UOW, returns one nullable server-policy candidate and creates no receipt. Actor, package/version, evidence and physical selectors are rejected before the UOW. Sync 2026-09-15.

Registry103 routes `picture-history.list` and `picture-history.full` through a dedicated OAuth `dream:read` Handler. Both read the canonical current actor inside the existing capability-checked Admin data UOW; the wire admits only nullable date bounds/limit or one exact date, creates no receipt and never adopts friendship or physical selectors. Sync 2026-09-15.

Registry101 routes two OAuth-only local-data writes through `localDataImportHandler`. POST uses the existing body limit and one receipt/audit transaction; original GET accepts only the operation and original request ID for the same service/subject, so response loss never blindly repeats Picture or Report inserts. Canonical owner and all physical selectors remain server-owned. Sync 2026-09-15.

Registry99 routes the sixteen Reflections operations through one named Handler. Seven user operations require service-bound OAuth and exact dream read/write scope; nine worker operations reject browser Authorization and require the configured service's `reflections:execute`. Original GET exposes only three OAuth writes by service+subject and eight background writes by exact `operation+task_id`; read operations and extra selectors are rejected. Existing Chat/SystemConfig/Session handlers admit `rta_` only through the six-name allowlist, matching Thread and metadata-only Session-list rule, and child write receipt recovery repeats subject/Thread binding. Sync 2026-09-15.

The existing internal `session.list` route may use a delegated bearer only after exact opaque-token parsing and full `DelegationService.resolve` checks for the configured service and `dream:read`; the resolved actor must be `server-persistence` with a Thread binding and no Editor binding. Cookie-bearing requests, wrong purpose/service/scope, expired or revoked grants, and every other Session operation fail before domain execution. OAuth list and exact `editor-stdio` load/replace behavior remain. This changes no Route file, DTO, operation hash, schema or Registry83 descriptor. Sync 2026-09-15.

Registry83 now routes OAuth-only `reflections-section-config.get/save/delete` after the Registry80 SystemConfig prefix. These operations accept only the closed section/raw prompt DTOs, require exact identity/unified capabilities, carry no entity scope, and retain raw stored JSON; save/delete use the existing same-UOW receipt/audit boundary. Original GET supports only save/delete operation plus original request ID and returns bounded stored results for the same service and subject. No actor/path/default/effective/SQL/Runtime selector is exposed. Focused registration/candidate tests passed in corrected57/57; named isolated public, five rollback faults and two actual final-COMMIT response-loss recoveries passed with product `console.error`0. Dream consumer wiring remains pending. Sync 2026-09-15.

Registry77 adds independent failure envelope POST/default fixed server codec and original OAuth-write GET. POST resolves original Run/Thread persistence grants using exact purpose capabilities; GET admits only operation/request and checks historical completion/current owner, no Runtime authority. Registration11 and public-harness static type/lint/diff passed;221 Markdown/MDX/356 decoded local links/247 historical routes have0 missing. PUBLIC READY sent and77 refrozen while primary owns isolated public/fault/COMMIT-loss execution. Prior76 descriptor and dispatch paths unchanged; Reflections/SystemConfig candidates unregistered. Earlier unregistered/pending stage receipts below retain their historical scope. Sync 2026-09-15.

Workspace76 independent closing actual fault90 and SELECT-only preservation159 passed after public347. Workspace/receipt/audit INSERT faults roll back all17 tables; actual final-COMMIT single response loss recovers GET/replay without duplicate effects, six owned fault objects cleaned. Primary released76 freeze; candidate failure/new Reflections remain unregistered. Normal business/model/Runtime/FS unexecuted. Sync 2026-09-15.

Registry76 delegates `workspace-default.ensure` to the thin OAuth-only Workspace handler; original GET admits only a single operation query and delegates current-owner/empty-input/all-null-scope validation. Exact identity/unified precede both UOWs, legacy text IDs stay opaque. Ingress11/original-domain9/static gates and primary-owned isolated public14 cases/22GET/347 assertions passed, including first same/distinct-original initialization and current owner checks. Independent fault90/actual COMMIT-loss and preservation159 passed; normal acceptance remains pending; root's candidate21 deterministic/source tests are separate. Sync 2026-09-15; primary released76 production/shared harness freeze.

Route Handlers only delegate to named domain handlers. Admin mutations require Origin/Session/RBAC/strict input and atomic audit. `/api/auth` delegates to installed Better Auth on capability-gated ORM UOW; `/api/internal/dream/v1` verifies configured service plus operation user/background scope and strictly projects DTOs. No SQL/table/column/caller-user dispatch or credential echo. Runtime renewal accepts only existing scoped opaque proof.

Workflow Run read/history and narrow start/fail/cancel are named domain handlers. Start requires service-bound OAuth; existing Run persistence grants retain original Run/Thread scope for failure/cancellation and bounded receipt recovery. Route bodies cannot assert readiness or actor identity.

Refs metadata management and Voice analysis use OAuth. Thread runtime refs/memory use exact Thread grants and the same owned domain repositories; memory writes scope receipts to that Thread. Artifact/CLI execution remains in Dream.

Preflight read/execute and user preferences use service-bound OAuth only. Preflight tokens are issued by Admin from the original stored six bindings; execute retains stage commits and bounded encrypted original recovery, with exact capabilities and live OAuth checks before every stage. Account preference writes accept five closed fields, with same-UOW receipt/audit and no first-login/system-config patch.

Nine Social operations route to the OAuth-only `socialFriendshipHandler`; original-request recovery uses the same per-operation kind/scope and exact output. All9 passed230 isolated assertions. No route table CRUD or service/actor selector is introduced. Preflight execute is registered; its dedicated original receipt validates absent/in-progress/committed evidence with current workspace ownership and exact encrypted bindings. Full public/stage fault verification remains pending.

Primary later completed independent remaining Preflight scopes and released freeze; original complete failures remain. Registry72 routes `workflow-run.create/retry` to the strict OAuth CreationHandler. Its original receipt GET only accepts one operation query and derives workspace/Run/Thread from the stored bounded result, repeating current ownership/frozen/source checks. Identity/unified requirements precede each UOW; no token or state patch authority is granted by recovery. Registration50 focused tests passed; actual creation/retry public validation remains pending.

Registry75 now also routes `dream-launch-source.ensure` and `dream-launch-dispatch.claim/finish` to closed OAuth-only handlers. Original GET admits only one operation query, repeats exact identity/unified and live write principal, then delegates to shared current-source/frozen/active-claim authority; no caller actor/workspace/Run/claim/metadata selector. Public thin POST3/GET7 gate and type/lint passed. Root independently verified Run72 remaining/fault/actual-COMMIT scopes; new75 isolated public component acceptance is pending and its production/harness bytes remain frozen. Source/claim/finish are separate original UOWs; prepare/failure recorder/full Runtime launch remain pending.
