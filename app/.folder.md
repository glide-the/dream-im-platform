# App folder contract

Next.js hosts protected Refine Admin, sole Better Auth `/auth` interactions and thin Admin/auth/internal Dream/Gateway resource ingress. `/admin` management permissions require live explicit Admin membership/RBAC; ordinary login is not management authority. All complex validation/persistence/audit stays under `app/lib`, all DDL in canonical Drizzle history; PostgreSQL only.
