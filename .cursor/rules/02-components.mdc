---
description: "Refine Admin 组件规则"
globs: ["app/components/admin/**/*.tsx", "app/components/admin/**/*.ts"]
---

# Admin 组件

- 组件只负责交互和展示，数据操作统一通过 Refine DataProvider 或受保护 Admin API。
- 不在客户端实现授权判断之外的安全边界；API 必须重复校验。
- 表单提供 label、错误/成功状态，破坏性动作需要明确确认。
- 列表使用服务端分页排序，不在浏览器拉取无限数据。
- Provider/Key/secret 不得在 UI 回显明文。
