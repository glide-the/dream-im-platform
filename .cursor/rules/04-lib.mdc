---
description: "Ink Memory 控制面领域层规则"
globs: ["app/lib/**/*.ts"]
---

# app/lib

- `admin/`：Session、RBAC、资源合同、写操作和审计。
- `billing/`：整数计价、账户、预授权、结算和账本。
- `gateway/`：协议、Provider、请求生命周期和 usage。
- `models/`：模型与定价解析。
- `db.ts`/`db/schema.ts`：PostgreSQL 连接和唯一 schema。

`app/lib` 不依赖页面或组件。写操作必须在事务中完成，外部输入必须先经严格 schema 验证。
