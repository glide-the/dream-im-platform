---
description: "通用能力层（app/lib）开发规则 — 核心稳定层"
globs: ["app/lib/**/*.ts"]
---

# 通用能力层规则 — `app/lib/**`

## 核心原则

**功能基本不变，结构尽量稳定**

`app/lib` 是共享能力中心，不应随功能迭代频繁改动结构与职责边界。

## 改动优先级

1. **复用现有文件与导出**（首选）
2. **小改动就地补强**（不拆目录、不换抽象）
3. **新增文件**（仅当重复代码明显或职责必须分离时）

## 现有模块职责

| 文件 | 职责 |
|------|------|
| `db.ts` | 数据库连接实例 |
| `db/schema.ts` | Drizzle schema 定义 |
| `queries.ts` | 通用查询函数 |
| `query.ts` | 查询工具函数 |
| `types.ts` | 共享类型定义 |
| `chat-schema.ts` | AI 对话协议 schema |
| `claude-agent-kit/` | Agent/模型侧封装 |
| `api-types.ts` | API 类型定义 |

## 禁止事项

❌ 大规模重构（除非明确需求：抽象重复/边界错误/测试覆盖充分）
❌ 依赖 pages/components
❌ 引入 UI 相关代码

## 依赖方向

```
lib → 外部包 ✅
lib → lib 内部其他模块 ✅
lib → pages/components ❌
lib → api ❌
```

## 新增模块检查清单

在新增文件前，确认：
- [ ] 现有文件无法承载此功能
- [ ] 至少有 2 处以上需要复用
- [ ] 职责边界清晰、命名合理
- [ ] 不会导致循环依赖
