---
description: "ink-memory-admin 项目概览与核心原则"
globs: ["**/*"]
alwaysApply: true
---

# ink-memory-admin

本仓库是 Next.js + Refine 的 Ink Memory 管理控制台，数据存储仅支持 PostgreSQL。

## 核心原则

- `/admin` 是唯一 UI，根路径跳转到 Admin。
- `app/api/admin` 与 `app/v1` 只做接入编排，领域能力位于 `app/lib`。
- 管理 API 服务端强制 Session、RBAC、Zod、事务和审计。
- 禁止恢复 `app/(app)`、ai4sales PWA、SQLite 或本地数据库回退。

## 结构

```text
app/(admin)/       Refine 页面
app/components/admin
app/api/admin      管理 API
app/v1             Claude/OpenAI 网关
app/lib            Admin/Gateway/Billing/DB 领域
drizzle            PostgreSQL 迁移
tests/e2e          Playwright
```
