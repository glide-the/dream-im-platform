---
description: "PostgreSQL 与 Drizzle 强约束"
globs: ["app/lib/db/**/*.ts", "app/lib/db.ts", "app/lib/**/*.ts", "drizzle/**/*"]
---

# 数据库规则

- PostgreSQL 是唯一存储；禁止 SQLite、文件 DB、内存 fallback。
- Schema 只在 `app/lib/db/schema.ts` 维护。
- Schema 变化必须运行 `pnpm db:generate` 并提交 SQL、snapshot、journal。
- 应用迁移使用 `pnpm db:migrate`；不得对未知数据库运行 push/drop/truncate。
- Route Handler 不写 SQL；SQL/事务位于对应 `app/lib` 领域模块。
- 财务金额使用整数 micro-USD；账本和审计不可原地修改。
