---
description: "改动指南与行为准则"
globs: ["**/*"]
alwaysApply: true
---

# 改动指南

## 最小改动原则

优先做"最小改动"：
- ✅ 不重排目录
- ✅ 不替换抽象
- ✅ 不引入与现有栈不一致的新依赖

## 遇到重复逻辑

1. 先判断是否能合并到 `app/lib` 的现有文件
2. 只有明显跨域复用才新增模块

## 改动前检查清单

- [ ] 是否理解了现有代码结构？
- [ ] 改动是否遵循目录职责边界？
- [ ] 是否复用了现有的 schema/类型？
- [ ] 是否需要补充测试？
- [ ] 改动是否保持向后兼容？

## 禁止事项（Hard No）

❌ 在 `app/api/**` 内实现大量领域逻辑或复制粘贴查询
❌ 在 UI 里引入"私有协议字段"导致前后端结构不一致
❌ 大规模重构 `app/lib`（除非明确需求：抽象重复/边界错误/测试覆盖充分）
❌ 引入与现有技术栈冲突的新依赖

## Commit 规范

使用 Conventional Commits：

```
feat: 新功能
fix: 修复 bug
docs: 文档更新
chore: 杂项（构建、依赖等）
refactor: 重构（无功能变化）
test: 测试相关
style: 代码格式（不影响逻辑）
```

## PR 提交要求

- 改动点列表
- UI 变更截图（如有）
- 覆盖的测试命令与结果

```bash
# PR 前必须通过
pnpm lint && pnpm test:run
```
