---
description: "Admin 与 Gateway Route Handler 规则"
globs: ["app/api/**/*.ts", "app/v1/**/*.ts"]
---

# API 层

Route Handler 只负责解析 path/query/body、调用 `app/lib` handler、返回响应。

- Admin 输入由领域层严格 Zod schema 校验。
- Admin API 必须服务端鉴权；mutation 还需 Origin、事务和审计。
- Gateway API 必须做 Key/scope/user/model/limit/balance 校验。
- 禁止在 route 文件复制 SQL、计费公式、密钥处理或 Provider 协议逻辑。
- 错误结构稳定且不泄露 secret、SQL 或内部堆栈。
