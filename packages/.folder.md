# packages/ — Folder Contract

- 本目录采用 pnpm workspace package 边界，结构参考 Paperclip；跨应用基础设施能力优先下沉到具名 package，Next.js `app/**` 只保留领域与兼容 facade。
- `db/` 是唯一数据库 package，拥有 client、schema、内嵌 PostgreSQL 和 migration runner；禁止再在根脚本或应用启动路径复制数据库生命周期。

## Sync

- 2026-08-21: 建立 workspace package 层，并新增 `@ink-memory/db`。
