# Test fixtures

SQL files are explicit isolated PostgreSQL harness input, never application startup DDL. `workflowRun.ts`, `workflowPreflight.ts` and `deckPluginCompatibility.ts` supply complete static original domain fields to provider-free DTO and service tests; no synthetic API or state machine is defined here. Fixture users and results are technical evidence, never real-business acceptance.
