<!-- [Sync] 2026-09-16: keep topology owner/mode validation portable across Darwin and GNU stat. -->
<!-- [Sync] 2026-09-16: project the exact Better Auth issuer, Dream resource and service registration into AutoDL. -->
<!-- [Sync] 2026-09-17: keep each deployment attempt immutable and package Drizzle at the release root. -->
<!-- [Sync] 2026-09-17: preserve service-client JSON when the generated env is sourced by bash. -->
<!-- [Sync] 2026-09-17: source public origins from deployment variables without a product-domain default. -->
<!-- [Sync] 2026-09-17: reconcile OAuth clients and verify a real service-token capability request on every release. -->
# deploy/autodl-ssh

## 定位

该目录是 Admin 在 AutoDL/SeetaCloud SSH 主机上的直接发布平台，与 `deploy/remote-ssh` 的 Docker、nginx、ECS 共享网络路径完全分离。

## 合同

- 代码、版本化 release 和配置位于 `AUTODL_APP_ROOT`；PostgreSQL 固定位于 Admin 服务用户拥有的 `/root/ink-autodl/data/postgres`，共享 Artifact 位于 Dream `AUTODL_DATA_ROOT/artifacts`。
- Admin 仅监听 `127.0.0.1:6008`，公网地址由 SeetaCloud 端口映射提供；脚本不安装或配置 nginx。
- `@ink-memory/db` 继续唯一管理嵌入式 PostgreSQL 和 Drizzle migration；应用启动不执行 DDL。
- PostgreSQL 及 Admin 以专用非 root 用户运行，`screen` 只负责断开 SSH 后的进程存活。
- Next.js 构建默认限制为 `AUTODL_BUILD_CPUS=1` 和 1024 MiB V8 old-space；DB runtime 优先硬链接复用 lock 与 package contract 完全相同的上一版不可变依赖树，依赖变化时的 fallback 打包使用独立的 `AUTODL_PACKAGE_MAX_OLD_SPACE_MB=384`；两项预算都可通过显式平台配置调整。
- `init-admin-data.sh` 幂等校正 owner/group/mode、拒绝符号链接，并在旧 Dream-root 或 `/var/lib/ink-memory` cluster 存在时 fail closed；它不会移动、删除或恢复真实数据。
- `prepare-env.sh` 从已有安全配置投影 `.env`，把 issuer 固定到 Admin `/api/auth`，并校验部署拥有的 Dream origin/callback/service secret；不会打印 secret，生成文件被 gitignore。
- `prepare-env.sh` 对部署生成的 service-client JSON 使用 shell 安全编码；合同测试必须实际 `source` 生成文件并比对 DTO 输入值，避免只检查文本时漏掉启动器的解析语义。
- `AUTODL_ADMIN_PUBLIC_ORIGIN` 与 `AUTODL_DREAM_PUBLIC_ORIGIN` 由当前实例的端口映射注入；示例只提供占位 origin，不把某个产品域名或某台 AutoDL 主机写成脚本默认值。
- `bootstrap` 只允许向零用户表目标导入本机嵌入式 PostgreSQL 的逻辑备份；常规发布使用 `deploy`。
- restore 后必须通过真实 `users` 行与业务表断言才允许 migration；失败重试可用 `AUTODL_BOOTSTRAP_DUMP` 复用远端备份。
- 发布期间旧应用仅用于候选失败恢复；完整验证后删除旧 release 与临时链接，不保留长期回滚点，migration 始终只前向执行。
- candidate ID 由 commit 与 UTC 时间组成，同一 commit 的重试不会覆盖 `current` 指向的目录；构建必须断言 `drizzle/meta/_journal.json` 位于 release 根目录，避免 Next standalone 占位目录造成嵌套。
- migration 后由发布步骤通过现有 OAuth catalog DTO/事务同步 browser、device 与 confidential service client；发布验证必须用 confidential client 实际换取 token 并读取受保护 capability。角色或 ACL 未按显式 cutover runner 激活、客户端目录缺失或数据连接失败时，发布失败关闭，不能只凭登录页 200 宣称成功。

## 入口

- `prepare-env.sh`：生成 AutoDL runtime env，并写入本地 gitignored `.env`。
- `platform.env`：从 `platform.env.example` 复制的、gitignored 的 SSH、公网映射和 Dream service registration 配置；`prepare-env.sh` 将它们投影为 issuer、trusted origins、resource 和 callback 绑定。
- `deploy.sh`：`check|plan|sync|build|bootstrap|deploy|migrate|start|stop|status|logs|verify|rollback`。
- `runtime/start-admin.sh`：screen 内的前台监督入口，不接受或记录明文 secret 参数。
- `runtime/verify-auth.mjs`：使用运行配置执行本机 `client_credentials` 与受保护 capability 探针，只输出状态码和脱敏标记。
- `runtime/init-admin-data.sh`：部署和独立栈启动共用的 Admin home/data 初始化器。
- `test-topology.sh`：provider-free 校验 env 投影、owner/mode、幂等性与旧数据/symlink 边界。

## Sync

- 2026-09-17: every AutoDL migration reconciles the typed OAuth catalog, and release verification proves limited-role auth plus protected capability access.
- 2026-09-17: deployment retries use unique immutable release ids and replace the standalone Drizzle placeholder before copying the migration journal.
- 2026-09-17: generated service-client JSON is shell-escaped and verified after sourcing by the same launcher semantics used remotely.
- 2026-09-17: AutoDL public origins remain required deployment inputs and the checked-in example contains no product-domain default.
- 2026-09-16: topology合同按Darwin/GNU显式选择`stat`语法，干净Linux CI可继续验证owner与mode。
- 2026-09-16: AutoDL runtime env pins `BETTER_AUTH_URL` to `/api/auth`, binds every service client to the selected Dream origin/callback and projects the shared Reflections directory without exposing a PostgreSQL credential to Dream.
- 2026-09-04: AutoDL releases copy and run the Provider migration orchestrator so every expand/data/contract gate completes before the new Admin starts.
- 2026-08-31: Record `https://ink-admin.suoxya.com` and `https://ink-frontend.suoxya.com` as the production public origins so regenerated runtime env keeps the Admin mutation allowlist correct.
- 2026-08-28: Reuse a lock-matched immutable DB runtime by hard link and retain a separately bounded fallback packaging path for dependency changes.
