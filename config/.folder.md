# Versioned policy configuration contract

- Registry83 historically added Reflections section-config get/save/delete. Corrected combined57/57, whole type/ownedlint/AST/JSON/Markdown/diff and prior80/PFreceipt/delegation parity passed. Named isolated public, five-fault rollback and two actual final-COMMIT response-loss recoveries also passed; raw stored prompt JSON and Dream-owned default/effective boundaries remain unchanged. Dream consumer validation is pending. Sync 2026-09-15.

- `reflections-section-policy.ts` is the registered custom-prompt policy with the original three section keys and five route-approved prompt field names. It contains no paths/secrets or static prompt content; Dream retains defaults/effective merging/FS/Agent. Sync 2026-09-15.

- 本目录保存需要被 schema、应用与运维 runner 共同读取的非 Secret 平台策略；禁止放置凭证、DSN 或用户数据。
- 429 Token 默认阈值以 `gateway-default-limits.mjs` 为唯一配置来源；修改 revision 或数值时必须新增前向 Drizzle migration 与显式数据 runner。
- 配置值必须是确定、可审计的安全整数；部署环境名称不得改变业务策略。
- `dream-domain-policy.ts` 读取明确 `DREAM_WORKFLOW_CONTEXT_MAX_ATTEMPTS` 操作容量，保留原 resolver 256 默认；容量只控制完整重试链的资源检查，不能改变业务权限或包装成产品上限。
- 同一文件读取 `DREAM_CHAT_AUTO_TITLE_MAX_CHARACTERS`，保留原50默认与Unicode字符计数；只影响首条普通user消息缺title时的自动投影，确认控制消息不写title或lease。
- 同一文件读取 `INK_DECK_HOST_COMPATIBLE`、`INK_CLAUDE_AGENT_CONTRACT_COMPATIBLE`、`INK_STORY_SCHEMA_COMPATIBLE`、`INK_DECK_RUNTIME_CONFIG_COMPATIBLE` 四项明确 server capability facts，缺失即不兼容；原 Python strip/大小写和1/true/yes/on语义保持，不按部署环境名称解锁业务。
- 同一文件读取 `DREAM_PREFLIGHT_MAX_INPUT_BYTES`，保留原canonical input的65536byte技术容量；不作为产品规则或权限门槛。
- `claude-plugin-marketplace-policy.mjs` 集中定义 Remote Marketplace Git host allowlist、同步超时和仓库/manifest/条目边界；不保存来源 URL、凭证或对象存储配置。
- `claude-agent-resource-policy.ts` 固定专用 `system_settings` JSONB 行、四项安全默认值、可空 SDK effort、正安全整数与组合内存 bytes 技术边界、20/60 秒 freshness 合同及精确 observer/Runtime capability；模型 Runtime int4 上界是存储技术边界；四项 admission 不设任意产品 min/max，0/null 不得表示无限或关闭保护；不保存 host、DSN、Bearer 或部署投影。

## Sync

- 2026-08-28: Added the SDK-backed effort enum, int4 model Runtime bound, and exact omit-when-unset capability contract.
- 2026-08-28: Claude Agent 四项策略移除 16/8192/4096/3600 与 128/64/5 历史产品边界，统一为正安全整数；run/reserve 合计只受 required-headroom bytes 精确表达边界，无需策略列类型 migration。
- 2026-08-19: 新增 ClaudePlugin Remote Marketplace v1 同步安全策略，默认仅允许 GitHub HTTPS 来源，部署可通过 capability 环境变量显式收缩或扩展 host allowlist。

- `DREAM_PREFLIGHT_TOKEN_TTL_SECONDS` 保留原正整数300秒默认； checking初始expiry与passed签发expiry分别从各自数据库clock计算，original encrypted receipt恢复不重设expiry或扩大权限。

- `workflow-run-policy.ts` 明确原Python Run协议255 Unicode codepoint的业务key规则；独立create/retry输入复用，既有shared Run输出须等PF冻结窗口结束且对照旧机器schema/hash后校正。

- `dream-launch-policy.ts` 保存原 launch command 的255/12000码点、80码点标题，以及标准UUIDv5 URL namespace/原source identity prefix和hidden metadata version。原派发claim TTL为五分钟，精确时间比较在固定pure codec；独立未注册source/dispatch按已验证actor与owned事实推导ID/claim/context，不接受request metadata或Run授权，也不承载Runtime配置。

- Primary解除PF冻结后，`workflow-run-policy.ts` 由stored Run输出与新create/retry输入共同消费；output只保留原model规则，request另保留原explicit Python blank校验。旧70完整契约/schema/hash保持，注册72不等于公开验收。

- `workspace-default-policy.ts` centralizes the original default Workspace label; empty settings remain the fixed creation shape. It contains no secret, identity, path or deployment switch. Sync 2026-09-15: initially unregistered primary-owned component.

- Sync 2026-09-15: primary default Workspace candidate unit20/source1 all21, type/lint/AST/diff exit0; production registration/public/consumer and normal acceptance remain pending. No existing module or registered75 bytes changed by this slice.

- 2026-09-15 Root-owned default Workspace candidate is now locally registered76; DTO/service headers corrected only, candidate21 retained. Producer ingress11+originalGET9/type/lint0 are separate; public concurrency/UOW/current-owner acceptance and deployment/consumer remain pending.

<!-- [Sync] 2026-09-15: independent Root SystemConfig candidate; no76 shared production/codec/schema edits. -->

`user-system-config-policy.ts`: original Settings codepoint/domain/path/env technical bounds and existing server-owned Runtime/TMP/actor keys; no secrets, identity or model quota.

<!-- [Sync] 2026-09-15: independent SystemConfig unit24/source1batch14 all25/type/lint/AST2/diff exit0; unregistered UI candidate only. -->

Root实际读Luna user-system-config-first-gate.md：new2test files/25passed、wholetsc0、ownedlint0、AST2/diff0；actual whole get/save14params/rawJSON/commit/close和明确nonobject/nonfinite failclosed冲突已验。未接fixedcodec map/Handler/OriginalGET/Registry，Thread/Run Runtime reader、public/concurrency/receipt/consumer与正常真实验收仍pending；不重复旧域门禁。
