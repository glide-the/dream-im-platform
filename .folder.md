# Admin repository contract

本目录是 Admin 管理控制台与 Anthropic/OpenAI 兼容 Gateway 的单一 Next.js 项目根目录。
`app/` 承载页面、公开 API 与业务模块；`packages/db/` 和 `drizzle/` 承载共享 PostgreSQL schema 与不可变迁移历史。
Next.js 从配置文件位置确定依赖解析根目录，启动与构建不应受仓库外锁文件或进程工作目录影响。

| 路径 | 职责 |
| --- | --- |
| `AGENTS.md` | 项目操作、产品设计、实现与验证约束 |
| `.github/` | provider-free 检查与隔离 PostgreSQL migration 发布门禁 |
| `README.md` | 项目能力、本机启动、配置与操作入口 |
| `package.json` | Admin 依赖与显式命令入口 |
| `pnpm-workspace.yaml` | 仓库内 workspace 包范围 |
| `next.config.js` | Next.js 根目录、隔离编译目录、NodeNext workspace源码解析与构建选项 |
| `postcss.config.js` | Tailwind CSS 的 PostCSS 集成 |
| `eslint.config.mjs` | 代码静态检查配置 |
| `vitest.config.ts` | 无 Provider 的业务合同测试配置 |
| `playwright.config.ts` | 浏览器技术验证配置 |
| `tsconfig.json` | Admin TypeScript 项目配置 |
| `tsconfig.base.json` | 仓库共享 TypeScript 基础配置 |
| `drizzle.config.ts` | 显式 schema/migration 工具配置 |
| `app/` | 管理页面、API、Gateway 与领域逻辑 |
| `packages/` | 共享数据库包 |
| `drizzle/` | 共享数据库迁移与数据迁移 runner |
| `scripts/` | 显式操作脚本与启动配置回归测试 |
| `tests/` | 测试初始化与浏览器验证 |
| `docs/` | 设计、架构、运维与验证文档 |
| `deploy/` | 发布与部署操作 |
| `docker/` | 容器配置 |

## Sync

- 2026-09-16: align GitHub validation with the current deterministic suite and immutable Drizzle migration entrypoint.
- 2026-09-16: `dev:app` 显式使用 Next Webpack；本机启动与 production build 共享 `next.config.js` 的 NodeNext extension alias，避免 Turbopack 将 workspace 源码中的 `.js` 说明符误判为缺失文件。

- 2026-09-15: `next.config.js` 为Webpack增加NodeNext extension alias，使`@ink-memory/db`开发态源码中的`.js`说明符解析到同目录`.ts/.tsx`；包发布和Node运行仍使用编译后的`dist/*.js`。

- 2026-09-15: `next.config.js` traces the fixed pure Deck, launch envelope, released failure envelope and registered80 SystemConfig scripts for the internal operation route. Runtime/FS execution remains in Dream; no configurable executable or database startup/migration behavior is added.

- 2026-09-15: Admin 0.1.1 aligns Remote Marketplace plugin digest ordering with Dream so approved nested skill trees install without false remote-drift failures.
- 2026-09-13: 启动说明补齐根目录修正后的旧 Turbopack 缓存处理：先停止服务并释放锁，备份 `.next` 后重建；不移动数据库或凭据文件，不初始化或迁移业务数据库。
- 2026-09-13: Next.js 使用配置文件所在目录作为绝对 Turbopack 根目录；新增独立 Node 配置回归测试，保留已有 Gateway 修复与全部数据库/启动合同。
