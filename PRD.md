# PRD｜AI for Sales（B2B 销售 SaaS）移动优先 PWA应用

## 1. 文档信息

* 产品名称：AI for Sales（移动优先 PWA）
* 形态：PWA（Mobile-first，支持添加到桌面/离线缓存/推送等按阶段实现）
* 目标用户：B2B 各行业销售（含销售主管/BD/大客户经理）
* 核心价值主张：用 AI 助手快速“找客户信息 → 结构化沉淀为客户档案 → 进入客户/待办管理”，提升销售信息收集与行动效率。

---

## 2. 背景与问题

### 2.1 典型痛点

* 销售在外跑客户，碎片时间多、录入成本高，客户信息散落在网页/微信/名片/图片里。
* “找到客户信息”和“沉淀为可用 CRM 客户档案”之间断层大。
* 行动项（TODO）与客户信息无法顺滑联动（信息搜完不知下一步做啥）。

### 2.2 机会点

* 用 Agentic Search 联网检索 + 结构化卡片二次确认，降低误入库风险，同时让沉淀成本极低。
* 移动端 PWA 适合销售高频使用场景（轻量、随时打开、可用语音/拍照）。

---

## 3. 产品目标（Goals）与成功指标（Metrics）

### 3.1 产品目标

1. 让销售在 30 秒内完成：输入“单位+姓名” → 获取客户卡片 → 确认入库。
2. 建立可用的客户列表与待办列表，支持搜索/排序/过滤，满足日常轻量 CRM 使用。
3. AI 助手成为入口：对话历史可追溯、可复用上下文（文件/图片/客户）。

### 3.2 核心指标（建议）

* 激活：新用户 24h 内完成“新增客户”≥1
* 转化：AI 助手触发检索 → 确认入库转化率
* 效率：从打开 AI 助手到成功入库的中位时长
* 留存：7 日/30 日留存；周活中使用 AI 助手比例
* 质量：客户卡片确认后被二次修改率（过高说明检索或抽取质量需优化）

---

## 4. 用户与场景

### 4.1 目标用户画像

* 一线销售：高频拜访、需要快速查客户背景并记录关键信息
* 大客户销售/BD：需要更丰富的客户画像（组织、联系人、决策链、近期动态）
* 销售主管（次要）：关注团队客户沉淀与行动执行

### 4.2 核心场景

1. 线下见客户前：快速补齐客户背景与联系方式
2. 会议后：通过语音/拍照补充客户信息，沉淀到客户档案
3. 跟进管理：客户列表检索/过滤 + 待办列表管理

---

## 5. 产品范围（Scope）

### 5.1 本期必须（MVP）

* PWA 基础框架（移动优先）
* 底部 3 Tab：待办列表 / 客户列表 / 我的
* 右下角悬浮球 → AI 助手首页
* AI 助手：输入单位+姓名 → 后端 Agentic Search → 返回客户资料卡片 → 新增/修改/取消
* 客户列表：卡片列表 + 分页 + 搜索 + 排序 + 过滤
* 待办列表：CRUD + 优先级 + 分页 + 搜索 + 排序 + 过滤
* 对话历史展示区（按会话/按时间）

### 5.2 后续迭代（不在 MVP 但预留）

* 客户与待办关联（对某客户创建待办、在待办里回溯客户）
* OCR 名片识别、企业工商/新闻动态增强、去重合并
* 团队版协作、权限、分配客户、数据看板
* 推送提醒、离线模式、语音转写优化

---

## 6. 信息架构与导航

### 6.1 底部 Tab（全局）

1. 待办列表页（Todo）
2. 客户列表页（Customers）
3. 我的（Me）

### 6.2 全局浮层入口

* 右下角悬浮球（Floating Action Button）
* 点击进入：AI 助手页面（独立路由，可从任意 Tab 进入）
* 悬浮球支持拖拽移动位置

---

## 7. 关键页面与交互需求

## 7.1 AI 助手首页（Agent Chat Home）

### 7.1.1 页面布局（从上到下分区）
1. 标题 + 欢迎语
2. 快捷输入卡片（Quick Prompts）
3. 对话历史展示区（History）, 默认折叠
4. 输入框 + 发送按钮（长按发送按钮切换语音输入）
5. 上下文选择区（Context Picker）
   * 选择文件上传
   * 选择相册
   * 拍照并上传照片
   * @ 拉起客户选择（列表 + 搜索框，支持选择特定客户作为上下文）

### 7.1.2 核心流程：新增/修改/取消客户

**触发条件**

* 用户在输入框输入：客户单位 + 姓名（例如“XX科技 张三”）
* 点击发送

**系统行为**

* 前端发起请求至后端 Agent 服务
* 后端执行 Agentic Search 联网获取客户资料并抽取结构化字段 + 生成非结构化补充信息（Markdown）
* 返回“客户资料卡片”给前端展示，供二次确认

**客户资料卡片（Customer Profile Card）内容结构**

* A. 结构化通用字段（示例）
  * 姓名（必填/尽量）
  * 联系方式（手机号/邮箱/微信等，可为空）
  * 单位/公司（尽量）
  * 地址（可为空）
  * 职位（可为空）
  * 备注标签（可为空）
* B. 非结构化信息（Markdown）
  * “更多维度信息”：例如公开背景、近期动态、可能的业务需求点、关系线索等（由 Agent 输出）

**卡片上的操作**

* 【确认新增】

  * 行为：创建客户 → 写入客户库 → 返回成功状态
  * UI：toast + “跳转到客户详情/客户列表”的提示（按产品策略）
* 【修改】

  * 行为：允许用户编辑结构化字段与 Markdown 内容（建议：字段编辑为表单，Markdown 为可编辑区域）
  * 保存后再【确认新增】（或直接“保存并入库”）
* 【取消】

  * 行为：不做任何入库操作；对话中保留这条记录（可标记为“未入库”）

### 7.1.3 输入与语音

* 文本输入：支持多行；回车行为（移动端通常为换行，发送用按钮）
* 长按发送按钮：进入语音输入模式
  * MVP 可先实现“长按提示 + 调用系统语音转文字”（具体实现依技术栈）
  * 语音转写完成后填入输入框，可编辑后发送

### 7.1.4 上下文选择区（Context）

* 文件：上传并作为本次消息上下文
* 相册/拍照：上传图片（用于客户资料抽取、名片/截图等）
* @客户选择：

  * 点击 @ → 弹出客户列表选择器（含搜索框）
  * 选定后在输入框插入引用（例如“@张三（XX科技）”）并带上 customer_id 作为上下文

---

## 7.2 客户列表页（Customers）

### 7.2.1 展示形态

* 卡片列表展示所有客户
* 卡片建议信息：姓名 + 公司/单位 + 职位（如有）+ 最近更新时间/标签（可选）

### 7.2.2 功能需求

* 分页（滚动加载或分页按钮，二选一）
* 搜索（按姓名/公司/联系方式/备注关键字）
* 排序（示例：最近更新、最近创建、姓名 A-Z）
* 过滤（示例：标签、是否有联系方式、行业/来源（若后续增加））
* 点击卡片进入客户详情

### 7.2.3 客户详情页（Customer Detail）

#### 7.2.3.1 页面结构（从上到下）

1. 顶部导航：返回 + 页面标题“客户详情” + 右侧【编辑】入口
2. 客户概览区：姓名、公司/职位、关键标签、最近更新时间
3. 基础信息（结构化）卡片区：以信息卡片形式展示字段（可折叠/可编辑）
4. 详情信息（非结构化）区：Markdown 内容按“块”展示（类似 Notion），可编辑
5. 底部操作区（编辑态）：【保存】/【取消】

#### 7.2.3.2 基础信息（结构化字段）要求

* 字段来源：Customer 结构化字段
* 展示样式：卡片化信息块（字段名 + 字段值）
* 建议字段：
  * 姓名
  * 公司/单位
  * 职位
  * 联系方式（手机号/邮箱/微信，可多条）
  * 地址
  * 标签
  * 备注/来源（可选）
* 编辑方式：
  * 点击【编辑】进入编辑态（字段变为表单输入/选择）
  * 支持新增/删除联系方式条目
  * 保存后立即更新详情页展示

#### 7.2.3.3 详情信息（Markdown）要求

* 字段来源：profile_markdown
* 展示样式：Markdown 按“块”渲染（段落、列表、引用、标题、标签块等）
* 编辑方式：
  * 编辑态下展示为可编辑 Markdown 区域（支持基本语法提示）
  * 保存后渲染为块状内容

#### 7.2.3.4 编辑与保存行为（MVP）

* 进入编辑态后：
  * 结构化字段可编辑
  * Markdown 可编辑
* 保存成功：
  * 回到只读态
  * 更新“最近更新时间”
  * toast 提示“客户信息已更新”
* 取消：
  * 放弃未保存修改，返回只读态
* 异常提示：
  * 保存失败提示重试

#### 7.2.3.5 一键收起与 AI 交互区块

* 支持一键收起【基础信息】与【详情信息】两个区块
* 收起后展示「已收起」提示条，提供【展开】入口
* AI Input Dock 常驻页面底部（不随收起/展开消失）
  * 允许继续提问/补充信息
  * 可选择文件/相册/拍照/@客户上下文
  * 默认 @ 当前客户（自动携带当前客户上下文到后端）
* 交互逻辑建议：
  * 默认展开
  * 用户点击【收起】→ 信息区折叠
  * 用户与 AI Input Dock 交互（如输入框开始输入）→ 自动折叠基础信息与详情信息
  * 点击【展开】恢复原信息展示

#### 7.2.3.6 客户详情页对话区（类似微信）

* 收起态下展示对话容器，承载用户与 AI 助手的消息往来
* 对话区样式：
  * 左侧为 AI 气泡，右侧为用户气泡（左右分开）
  * 支持滚动查看历史消息
  * 最新消息靠近 Input Dock，便于继续追问

#### 7.2.3.7 Input Dock @客户显示

* contextRow 的 @ 客户直接展示当前客户名称（例如“@张三”）
* 默认已选中当前客户，发送消息时自动携带 customer_id

---

## 7.3 待办列表页（Todo）

### 7.3.1 功能需求

* TODO Item：增删改查（CRUD）
* 优先级：至少 3 档（高/中/低）或 4 档（P0-P3）
* 分页
* 搜索（标题/描述关键字）
* 排序（优先级、创建时间、更新时间）
* 过滤（优先级、状态：未完成/已完成）

### 7.3.2 数据字段（建议）

* 标题（必填）
* 描述（可选）
* 优先级（必填，默认中）
* 状态（未完成/已完成）
* 截止时间（后续可加，MVP 可不做）
* 关联客户（后续迭代；MVP 可不做）

---

## 7.4 我的（Me）

* 账号信息（姓名/公司/角色）
* 设置（语言、数据导出、隐私与协议）
* 反馈入口

---

## 8. 业务规则与边界条件

### 8.1 客户入库校验

* 最低入库条件（建议）：至少包含【姓名 或 单位】中的一个
* 去重策略（建议 MVP 简化）：

  * 若姓名+单位完全相同 → 提示可能重复（可让用户继续新增或合并，后续再做）
* 修改操作仅影响“即将入库”的卡片内容；入库后修改走客户详情编辑（后续可加）

### 8.2 Agentic Search 返回异常

* 无结果：提示“未找到可靠信息”，给用户选项：

  * 手动新增（跳到空白客户卡片编辑）
  * 重新搜索（建议用户补充更多关键词）
* 结果不可信/冲突：卡片标注“信息来源不确定”，优先让用户修改确认

### 8.3 文件/图片上传限制（建议）

* 单文件大小限制、类型限制（PDF/图片等）
* 上传失败重试、弱网提示

---

## 9. 数据模型（建议）

### 9.1 Customer

* id
* name
* company
* title
* phones[] / email[] / wechat（可选）
* address
* tags[]
* profile_markdown（非结构化）
* created_at / updated_at
* source（ai_search / manual / import）
* last_verified_at（可选）

### 9.2 Todo

* id
* title
* description
* priority
* status
* created_at / updated_at

### 9.3 Chat / Conversation

* conversation_id
* messages[]（role, content, created_at）
* attachments[]（file/image refs）
* linked_customer_id（可选）
* ai_outputs（如客户卡片 JSON）

---

## 10. 接口需求（API，示例级）

### 10.1 AI 客户检索

* `POST /agent/search-customer`

  * body: { query_text, attachments?, context_customer_ids? }
  * resp: { customer_card: { structured_fields, profile_markdown, confidence?, sources? }, action_suggestions? }

### 10.2 客户

* `POST /customers`（创建）
* `GET /customers`（分页+搜索+排序+过滤）
* `GET /customers/{id}`
* `PATCH /customers/{id}`
* `DELETE /customers/{id}`（可选，MVP 可不开放删除或做软删除）

### 10.3 待办

* `POST /todos`
* `GET /todos`（分页+搜索+排序+过滤）
* `PATCH /todos/{id}`
* `DELETE /todos/{id}`

---

## 11. 非功能需求（NFR）

* 性能：移动端首屏加载快；列表滚动与搜索响应可接受
* 可靠性：Agent 超时/失败要有明确状态与可重试
* 安全：

  * HTTPS
  * 鉴权（Token）
  * 文件上传安全扫描/类型校验
* 隐私与合规：

  * 明确提示“联网检索可能涉及公开信息”
  * 客户数据为企业数据，提供导出/删除策略（按地区法规逐步完善）
* 可观测性：

  * 关键埋点（触发检索、返回卡片、确认新增、取消、修改保存、上传成功/失败）
  * 后端日志与 trace（Agent 调用链路）

---

## 12. 关键状态与文案建议（MVP）

* AI 检索中：加载态 + “正在联网检索客户信息…”
* 无结果： “暂时没找到可靠信息，你可以补充关键词或手动新增”
* 卡片待确认： “请确认后入库（可修改）”
* 新增成功：toast “客户已新增”，并出现“去客户列表/查看详情”的跳转提示
* 取消：toast “已取消，未新增客户”

---

## 13. 里程碑

* M1：PWA 框架 + 3 Tab + 基础列表（客户/待办）CRUD
* M2：AI 助手页面 + 对话历史 + Agentic Search 返回客户卡片 + 入库闭环
* M3：上传上下文（相册/拍照/文件）+ @客户选择器
* M4：排序/过滤完善 + 去重/合并 + TODO 与客户关联（迭代）
